# 과제소개 — 나만의 자비스: 로드맵 브리핑

**강남대학교 네트워크서비스 (2026-2) · 2주차 과제**

---

## 1. 해결할 문제

`study-progress/` 안에는 로드맵이 8개 있다. 인공지능융합공학부 로드맵도 통합 커리큘럼을 순서대로
밟으라는 뜻이 아니라, 다른 7개(네트워크 서비스, DB 설계, 데브옵스, PM 등)와 마찬가지로 **각각 독립된
과목**일 뿐이다. 여러 과목을 동시에 듣다 보면 로드맵별로 어디까지 했는지, `내학습/복습큐.md`에 쌓인
오답 중 오늘 뭐부터 다시 봐야 하는지가 폴더마다 흩어져서 한눈에 안 보인다. 세션을 새로 열 때마다
"오늘 뭐부터 하지"를 매번 파일을 뒤져서 다시 판단해야 했다.

## 2. 선택한 도구

Claude Code. 강남대 네트워크서비스 수업에서 배포받은 학습 시스템 저장소
(`https://github.com/dlrlwjd1/new_education_loop`)를 그대로 확장했다. `CLAUDE.md`와
`.claude/skills/` 스킬 구조를 그대로 사용한다.

## 3. 배포본에서 바꾼 점

| 파일 | 배포본 | 바꾼 점 |
|---|---|---|
| `CLAUDE.md` | `/study` `/roadmap` 두 스킬과 그 우선순위만 정의 | "이 자비스가 돕는 것" 절을 새로 추가해 각 로드맵이 독립 과목이라는 전제를 명시하고, `/브리핑` 스킬을 세 번째 스킬로 추가. "기록" 절에 `내학습/브리핑로그.md`를 추가 |
| `.claude/skills/브리핑/SKILL.md` | (배포본에 없음, 신설) | 로드맵별 진행률 계산 + 복습 우선순위 추출 + 기록을 담당하는 새 스킬. `/study`(질문·채점)나 `/roadmap`(로드맵 신설)과 역할이 겹치지 않도록, "판단 재료만 모아 보여준다"로 경계를 명확히 그었다 |
| `내학습/브리핑로그.md` | (배포본에 없음, 신설) | `/브리핑` 실행 결과를 날짜별로 누적 기록하는 새 상태 파일 |

이름만 바꾼 것이 아니라, 기존 `/study`·`/roadmap`이 다루지 않는 **"여러 로드맵을 가로질러 오늘 뭐부터
볼지 판단할 재료를 모은다"**는 새 기능을 스킬 하나로 추가했다.

## 4. 핵심 파일의 역할

- `CLAUDE.md` — 이 저장소 전체의 프로젝트 지침. 누구를 돕는지, 어떤 결정(로드맵 신설/삭제)은 사용자에게
  물어보는지가 여기 있다.
- `.claude/skills/브리핑/SKILL.md` — `/브리핑`의 행동 매뉴얼. 무엇을 읽고, 어떤 순서로 계산하고,
  어디에 기록하는지, 정보가 부족할 때 무엇을 하는지가 절차로 적혀 있다.
- `study-progress/*/*Phase*.md` — 각 로드맵의 진행 기록 원본. 체크박스가 `/study`에 의해 갱신된다.
  `/브리핑`은 이 체크박스를 세기만 하고 고치지 않는다.
- `내학습/복습큐.md` — `/study`가 쌓는 오답 큐. `/브리핑`은 "다음 복습일"이 오늘이거나 지난 행만 읽어서
  우선순위로 보여준다.
- `내학습/브리핑로그.md` — `/브리핑`을 실행할 때마다 한 줄씩 남는 기록. 다음 실행에서 "지난번엔 뭐가
  밀려 있었는지"를 비교할 수 있는 원본이다.
- `내학습/진행상황.md` — `/study` 세션 기록(배포본 그대로 사용).

## 5. 실행 방법

```bash
cd new_education_loop
claude
```

세션 안에서:

```
/브리핑
```

인자 없이 실행하면 `study-progress/` 안의 모든 로드맵을 대상으로 한다. 특정 로드맵만 보고 싶으면
`/브리핑 네트워크 서비스 로드맵`처럼 이름을 준다.

**성공 기준**: 실행하면 (1) 오늘 기준 밀린 복습 항목이 마감 지난 순서로 나오고, (2) 로드맵별 진행률이
"완료/전체"와 함께(반올림으로 0%에 묻히지 않게) 나오고, (3) `내학습/브리핑로그.md`에 한 줄이 새로
추가되면 성공이다.

자세한 사용 기록과 개선 과정은 `사용과개선.md`에 있다.
