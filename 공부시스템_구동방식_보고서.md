# 공부 시스템 구동방식 보고서

> 웹 구현(디자인 시스템, 웹 전환 기능명세 등)은 이 보고서의 범위에서 제외한다.
> 이 문서는 `.claude/skills/`와 `study-progress/`, `내학습/`, `courses/`로 구성된
> **CLAUDE Code 기반 공부 시스템**이 실제로 어떻게 동작하는지만 다룬다.

## 1. 개요

이 저장소는 강남대학교 네트워크서비스 수업에서 배포된 **학습 시스템**이다. 핵심 전제는
"AI가 답을 알려주면 학생은 이해했다고 착각한다"는 것이며, 시스템 전체가 이 전제를
막기 위한 절차로 짜여 있다. AI(자비스)의 역할은 가르치는 것이 아니라 **시험관** 역할이고,
실제 학습(인출)은 학생이 한다.

시스템은 세 개의 슬래시 스킬(`/roadmap`, `/study`, `/브리핑`)과 세 종류의 저장소
(`study-progress/`, `courses/`, `내학습/`)로 이루어진다. 대화 자체는 휘발되지만, 각
스킬이 끝날 때마다 파일에 기록을 남기고 커밋을 권하기 때문에 **진행 기록의 원본은 항상
파일**이다.

## 2. 지키는 규칙 (CLAUDE.md)

저장소 루트의 `CLAUDE.md`가 자비스의 행동 규범이다. 핵심은 네 가지다.

| 규칙 | 의미 |
|---|---|
| 한국어로 답한다 | 기술 용어만 필요시 영어 병기 |
| 답을 먼저 주지 않는다 | 질문으로 꺼내게 한 뒤에만 채점한다 — 읽어서 익숙해진 느낌(유창성의 착각)은 이해가 아니라고 명시 |
| 과제를 대신 풀지 않는다 | 막힌 지점을 좁혀주는 것까지가 AI의 일 |
| 진행 기록은 파일로 남긴다 | 대화는 사라지고 파일은 남는다는 전제 |

## 3. 저장소 구조와 각 폴더의 역할

```
study-progress/{로드맵 이름}/     ← 로드맵(=과목). 각 로드맵은 독립적이다
  README.md                       ← 도착점 · Phase 표 · 총 소요 시간
  01 Phase 1 - {이름}.md          ← 체크박스가 있는 실제 진행 문서 (진행 기록의 원본)
  ...
courses/                          ← 강의 자료 정리본. 읽기 전용으로 취급
내학습/
  진행상황.md                     ← 세션마다 한 줄씩 쌓이는 학습 로그
  복습큐.md                       ← 틀린 항목과 다음 복습일 (간격 반복 스케줄)
  브리핑로그.md                   ← /브리핑을 돌릴 때마다 한 줄씩 쌓이는 기록
.claude/skills/
  roadmap/SKILL.md                ← 로드맵을 새로 설계하는 절차
  study/SKILL.md                  ← 실제 학습·시험 세션을 진행하는 절차
  브리핑/SKILL.md                 ← 전체 현황을 모아 보여주는 절차
```

로드맵 중 "인공지능융합공학부 로드맵"은 학부 4년 전공 전체를 덮는 지도이지만,
다른 로드맵들(네트워크 서비스, DB 설계, 데브옵스 등)과 동급의 **하나의 과목**일 뿐,
통합 커리큘럼으로 순서대로 밟으라는 의미는 아니다.

## 4. 전체 순환 구조

세 스킬은 하나의 순환 고리를 이룬다.

```
/roadmap  ──(로드맵을 새로 설계)──▶  study-progress/{로드맵}/
                                          │
                                          ▼
/study    ──(Phase를 하나씩 시험 보며 통과)──▶  체크박스 갱신 + 내학습/복습큐.md·진행상황.md 갱신
                                          │
                                          ▼
/브리핑   ──(모든 로드맵의 진행률 + 오늘 복습 대상을 한 화면에)──▶  내학습/브리핑로그.md 갱신
                                          │
                                          └──(사용자가 다음에 뭘 할지 스스로 판단)──▶ 다시 /study
```

세 스킬은 역할이 겹치지 않도록 명확히 분리되어 있다.

- `/roadmap`은 **로드맵 구조를 만들거나 고친다.** 학습을 진행하지 않는다.
- `/study`는 **실제로 학습·시험을 진행하고 체크박스·복습큐를 갱신한다.** 로드맵을 새로 만들지 않는다.
- `/브리핑`은 **현황을 모아 보여주기만 한다.** 체크박스도, 복습큐도, 로드맵 폴더도 건드리지 않는다.
  "오늘 뭘 하세요"라고 대신 정해주지도 않는다 — 판단은 사용자의 몫이다.

## 5. 스킬별 동작 방식

### 5.1 `/roadmap` — 내 로드맵 만들기

받은 커리큘럼을 그대로 쓰지 않고, 사용자의 전공·학년·목표에 맞춰 로드맵을 새로 설계한다.
순서가 핵심이다.

1. **도착점을 한 문장으로 확정한다.** "이해한다"는 금지 — 반드시 결과물로 적는다
   (예: "내 서버에 도메인을 붙이고 HTTPS로 열어서 한 달간 운영한다").
2. **`courses/`를 보기 전에** 도착점에서 거꾸로 내려오며 필요 지식을 20~40개 항목으로
   나열하고 사용자 확인을 받는다. 자료 먼저 보고 목차를 짜면 "가진 자료로 만들 수 있는
   로드맵"이 되어버리기 때문에 순서를 강제한다.
3. 항목들을 선행 관계로 묶어 4~8개 Phase로 정리한다.
4. 그제서야 `courses/`를 검색해 강의를 매칭하고, 자료가 없는 항목은 "산출물 과제"로 채운다.
5. `study-progress/{주제} 로드맵/`에 README와 Phase 문서(체크박스 포함)로 저장하고 커밋한다.

### 5.2 `/study` — 답을 주지 않고 질문을 시킨다

가장 핵심적인 스킬이다. 입력이 로드맵/Phase명이면 **자료 경로**, 그 외 주제명이면
**주제 경로**로 갈리지만 이후 로직은 합류한다.

- **자료 경로**: 로드맵 폴더에서 체크 안 된 첫 항목을 찾고, AI가 먼저 자료를 읽은 뒤
  제목만 보여주고 "뭘 다룰 것 같냐"고 먼저 묻는다.
- **주제 경로**: 자료를 열기 전에 "지금 아는 것을 적어보라"고 먼저 시킨다 (사전 인출).

이 답을 보고 **재료 유무를 판정**한다. 재료가 있으면 바로 인출 루프로 들어가고, 재료가
없으면(모르겠다/전부 틀림) "지금은 설명 단계로 간다"고 명시적으로 밝힌 뒤 5분 이내 분량만
짧게 설명하고 곧바로 다시 꺼내게 한다. 인출 루프는 한 라운드에 질문 하나만 던지고, 답을
**맞음 / 유창하지만 틀림 / 모름** 세 가지로 채점한다. 특히 "유창하지만 틀림"을 가장
중요한 판정으로 취급해 정확히 짚어준다. 같은 항목에서 힌트가 세 번 나가도 안 되면
"재료 부족"으로 되돌아간다.

낱개 개념이 맞기 시작하면 "왜 그렇게 설계했는가"로 정교화 질문을 던져 청킹(덩어리화)을
유도하고, 세션 중간·끝에는 복습큐에서 다른 항목을 섞어 인터리빙을 강제한다. 세션이
끝나면:

- **자료 경로였다면** Phase 문서의 체크박스를 `[x]`로 바꾸고 날짜를 적는다
  (진행 기록의 원본은 항상 Phase 문서의 체크박스다).
- 틀린 항목만 `내학습/복습큐.md`에 추가한다. 복습 간격은 **1일 → 3일 → 7일 → 16일 → 35일**이며,
  맞히면 다음 칸으로, 틀리면 1일로 되돌린다.
- `내학습/진행상황.md`에 날짜·내용·맞은/틀린 개수를 한 줄 남긴다.
- 사용자가 직접 세 줄로 요약하게 한다(AI가 요약하지 않음) — 이것이 그날의 마지막 인출 연습이다.
- 커밋을 권한다.

### 5.3 `/브리핑` — 흩어진 진행 상황을 한 화면으로

로드맵이 여러 개로 늘어나면 어디까지 했는지 폴더마다 흩어져 안 보이게 되는 문제를 다룬다.
판단 재료만 모아 보여주고, 무엇을 공부할지는 정해주지 않는다.

1. 대상 로드맵마다 모든 깊이에서 "Phase"가 들어간 `.md` 파일을 찾아 `[ ]`/`[x]` 체크박스
   개수를 센다. Phase 문서가 없는 로드맵은 "진행 자료 없음"으로 표시하고 건너뛴다
   (임의로 로드맵을 만들거나 지우지 않음 — 그건 `/roadmap`의 일).
2. **정수 반올림을 쓰지 않는다.** 수백 개 항목 중 2~3개만 체크해도 정수 %로는 "0%"가 되어
   "시작 안 함"과 "시작은 했음"이 구별되지 않는 문제가 실사용에서 발견되었기 때문에,
   완료 0개는 "0% (미시작)", 소수 첫째 자리도 0.0이면 "<0.1% (진행 중, n/전체)"로 구분해
   표시한다.
3. `내학습/복습큐.md`에서 다음 복습일이 오늘이거나 지난 항목만 뽑아, 가장 오래 밀린 것부터
   보여준다.
4. 오늘 날짜·복습 우선순위 표·로드맵별 진행률 표 순으로 화면에 보여주고, 판단(오늘 뭐부터
   할지)은 사용자에게 맡긴다.
5. `내학습/브리핑로그.md`에 날짜·밀린 복습 개수·진행 중 로드맵 수·평균 진행률·메모를
   한 줄 추가하고 커밋을 권한다.

## 6. 기록과 버전관리

모든 스킬은 세션 끝에 **파일 갱신 + git 커밋 권유**로 마무리된다. 세 개의 기록 파일이
서로 다른 시점의 상태를 나타낸다.

| 파일 | 기록 주체 | 내용 |
|---|---|---|
| `study-progress/*/0N Phase *.md`의 체크박스 | `/study` | 무엇을 실제로 통과했는가 (원본) |
| `내학습/진행상황.md` | `/study` | 세션별 학습 로그 (날짜·내용·맞은/틀린 개수) |
| `내학습/복습큐.md` | `/study` | 틀린 항목의 간격 반복 스케줄 |
| `내학습/브리핑로그.md` | `/브리핑` | 브리핑을 돌린 시점의 스냅샷 |

세 스킬이 서로의 기록 파일을 침범하지 않도록 "절대 하지 않는 것" 목록이 각 SKILL.md에
명시되어 있다 (예: `/브리핑`은 체크박스나 복습큐를 직접 고치지 않고, `/study`는 로드맵
폴더를 새로 만들지 않는다).

## 7. 설계 철학 요약

이 시스템은 학습과학의 다섯 전략(인출 연습, 간격 반복, 인터리빙, 청킹, 정교화)을
병렬이 아니라 **의존 구조**로 실행한다: 인출 연습과 간격 반복이 "재료를 살아있게" 만드는
선행 조건이고, 그 위에서 정교화가 청킹을 만든다("정교화는 청킹의 공장이다"). 인터리빙은
그 위에서 "어떤 방법을 쓸지 고르는 연습"으로 작동한다.

이 구조를 지키기 위한 절대 금지선은 하나로 요약된다 — **사용자가 답하기 전에는 답을
쓰지 않는다.** 순서를 바꾸면 인출 연습이 아니라 그냥 읽기가 되기 때문이다.
