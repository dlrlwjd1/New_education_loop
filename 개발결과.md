# 개발 결과

헌법(`.specify/memory/constitution.md`) 원칙 VII에 따라, 기능 하나가 원칙 VI(메인 에이전트 품질 검수)를 통과해 완료로 확정될 때마다 이 파일에 한 섹션씩 추가한다.

---

## 2026-09-18 — 자료 이전 기반 (Content Ingestion Foundation)

**대응 스펙**: `specs/001-content-ingestion-foundation/`(spec.md, plan.md, data-model.md, contracts/ingestion-library.md, tasks.md T001~T033 전부 완료)

**변경 범위**: `backend/` 신규 생성 — 저장소에 처음 만들어진 백엔드 패키지.

- `backend/src/ingestion/`: `types.ts`, `checkboxNormalize.ts`, `markdownAst.ts`, `resolveIdentity.ts`, `parseRoadmaps.ts`, `parseMaterials.ts`, `materialIndex.ts`, `resolveMaterialLinks.ts`, `exampleSeparation.ts`, `runImport.ts`, `cli.ts`
- `backend/tests/`: 단위 테스트 4개(`checkboxNormalize`, `resolveIdentity`, `materialIndex`, `contractConformance`), 통합 테스트 7개(`parseRoadmaps.basic`, `parseRoadmaps.nestedTracks`, `parseMaterials.count`, `resolveMaterialLinks`, `runImport.dedupe`, `runImport.scopeSeparation`, `edgeCases`), 픽스처 6종
- `backend/package.json`, `tsconfig.json`, `eslint.config.mjs`, `.prettierrc.json`
- 루트 `.gitignore` 신설(node_modules/dist/build 등)
- 스펙 문서 갱신: `spec.md`(FR-007 축소, FR-010/012 정리, 링크 깨짐 엣지케이스·US3 시나리오 추가), `data-model.md`(Material.provider/course, link_broken 검증 규칙), `contracts/ingestion-library.md`(구현 중 확정된 시그니처 변경 3건 반영), `tasks.md`(전체 완료 표시)

**담당 서브에이전트**: 백엔드 구현 1개(설정·재단·US1~US4 구현), QA 검증 1개(구현과 분리해 계약 문서만 근거로 테스트 작성·실행) — 백엔드 서브에이전트를 2회 호출(1차 구현, QA가 찾은 버그 수정을 위한 2차 재작업).

**검증 방법과 결과**:

- `npx tsc --noEmit`: 클린(0 오류) — 메인 에이전트가 직접 재실행해 확인.
- `npx eslint src`: 클린 — 메인 에이전트가 직접 재실행해 확인.
- `npx vitest run`: **11 test files / 65 tests 전부 통과** — 메인 에이전트가 직접 재실행해 확인.
- 실제 저장소(`study-progress/` 8개 로드맵, `courses/` 7,865개 자료) 대상 `runImport({scope:"real"})` 직접 실행 결과, 메인 에이전트가 독립적으로 재실행해 확인: `roadmapCount=8, materialCount=7865, duplicateCount=0, exampleItemCount=0, mappings=14571, errors=0` — 백엔드·QA 두 서브에이전트가 각자 보고한 수치와 정확히 일치.
- SC-001(완료/전체 100% 일치): QA가 `grep -rE '^\s*-\s*\[[xX ]\]'`로 로드맵별 원본 체크박스 수를 독립 재계산해 8개 로드맵 전부 정확히 일치 확인(부록·인덱스 문서 제외, 코드 블록 오탐 없음도 별도 확인).
- SC-002(중첩 트랙 누락 0건), SC-003(자료 수 7865 일치), SC-004(재실행 중복 0건, 매핑 14,571건 두 실행 간 완전 동일), SC-006(한글·공백·URL 인코딩 경로 100건 표본 오류 0건): QA가 실제 저장소 기준으로 독립 검증.
- SC-005(예시 데이터 0건): 실저장소에는 예시 표시가 없어 이 조건은 현재 공허하게 참(vacuous pass) — QA가 만든 픽스처(`scope-separation/`)로 실제 분리 로직 자체를 검증했고, 그 과정에서 진짜 버그 2건을 발견.

**품질 검수(원칙 VI)에서 발견해 재작업으로 되돌린 실제 결함**:

1. `runImport()`의 `ImportBatch.mappings`가 `scope`로 필터링되지 않아 예시 데이터의 매핑이 "실제" 결과에 새어 들어가는 버그 — QA가 픽스처로 재현, 백엔드가 `applyScope()`에 매핑 필터링을 추가해 수정.
2. `metrics.exampleItemCount`가 계약이 정의한 "결과에 남은 예시 항목 수"가 아니라 "제외된 항목 수"를 세고 있던 버그(같은 근본 원인) — 같은 수정으로 해결.
3. `backend/tsconfig.json`의 `rootDir`/`include` 충돌로 테스트 파일이 생기자마자 `tsc --noEmit`이 깨지는 설정 결함 — `rootDir` 제거로 수정.

세 건 모두 메인 에이전트가 직접 diff를 읽고 재검증(`tsc`/`vitest`/실저장소 재실행)한 뒤 반영을 확인했다. 서브에이전트의 "완료했다" 보고를 그대로 신뢰하지 않고, 이번 항목처럼 구체적 재현 조건이 있는 경우에만 통과로 처리했다.

**구현 중 발견해 계약 문서에 반영한 사양 결정** (자세한 근거는 `contracts/ingestion-library.md`, `spec.md` Assumptions 참고):

- `resolveMaterialLinks`에 `roadmaps` 파라미터 추가(원안 시그니처로는 FR-013을 채울 수 없었음).
- `parseRoadmaps`/`parseMaterials`/`runImport`에 테스트용 `now`/`studyProgressRoot`/`coursesRoot` 옵션 추가.
- `resolveIdentity`의 `previousBatch`는 이번 실행 자체의 누적 원장을 자기 자신에게 넘기는 용도가 아님을 명시(실제 `courses/`에서 내용이 같은 서로 다른 파일 21건을 잘못 병합하는 버그를 실행 중 발견해 수정).
- FR-011 "예시 데이터" 판별을 위한 최소 표시 규약(`<!-- scope: example -->`) 신설 — 저장소에 기존 기계 판별 표시가 전혀 없었음.
- FR-007의 "주제(topic)" 기반 검색은 신뢰할 수 있는 근거가 없어 범위에서 제외, `provider`/`course`는 경로 구조 기반 추론 규칙을 구체적으로 확정.

**다음 단계**: 이 기능은 파싱·식별자·색인 규칙의 정의와 검증까지만 다룬다(FR-016). 3단계(사용자·읽기 화면)에서 `contracts/ingestion-library.md`의 인터페이스를 그대로 재사용해 영속 저장소(SQLite 파생 캐시)를 구축하는 작업이 이어진다.

---

## 2026-09-22 — 영속 저장소 계층 (Persistence Layer)

**대응 스펙**: `specs/002-persistence-layer/`(spec.md, plan.md, research.md, data-model.md, contracts/persistence-library.md, quickstart.md, tasks.md T001~T033 전부 완료)

**변경 범위**: `backend/src/persistence/` 신규 생성 — 001의 파싱 계약을 SQLite 파생 캐시에 적재하고 조회 함수를 제공하는 새 하위 패키지. `backend/src/ingestion/`(001)은 변경하지 않았다.

- `backend/src/persistence/`: `types.ts`(계약 반환 타입), `schema.ts`(전체 DDL + `PRAGMA user_version`), `db.ts`(`node:sqlite` 오픈 헬퍼 + 임시 파일 빌드 후 원자적 교체), `load.ts`(`ImportBatch` → 테이블 적재, 적재 실행 기록), `queries.ts`(`reload`/`listRoadmaps`/`getRoadmapDetail`/`searchMaterials`/`listReviewNeededItems`)
- `backend/tests/unit/`: `schema.test.ts`, `db.test.ts`, `contractConformance.persistence.test.ts` 신규 3개
- `backend/tests/integration/`: `persistence.load`, `persistence.queries.performance`, `persistence.reload.fileWins`, `persistence.reload.dedupe`, `persistence.rebuild.deleted`, `persistence.rebuild.corrupted`, `persistence.reload.atomicFailure`, `persistence.queries.roadmapDetail`, `persistence.queries.materialSearch`, `persistence.queries.reviewNeeded` 신규 10개
- `backend/tests/fixtures/persistence-reload/`, `persistence-review-needed/`: 이 기능 전용 신규 픽스처(001의 기존 픽스처는 그대로 재사용)
- 루트 `.gitignore`: `backend/.cache/`(SQLite 파생 캐시) 추가
- 스펙 문서 갱신(구현 중 발견한 것 반영): `contracts/persistence-library.md`(`searchMaterials`에 `sourcePath` 필터 추가 — FR-004가 명시한 "경로" 검색을 원안이 누락했었음; `reload()` 동작 설명에서 `previousBatch` 단계 제거), `research.md` §3(재적재 신뢰성 재확인), `data-model.md`(`load_run_file_snapshots`/`material_versions` 설명을 실제 동작에 맞게 수정), `quickstart.md`(자료 제목 검색 예시를 정확 일치 의미에 맞게 수정), `tasks.md`(Revision note + T016/T018/T022 재작성)

**담당 서브에이전트**: 백엔드 구현 1개(Setup·Foundational·US1~US4 구현 — 테스트 제외), QA 검증 1개(구현과 분리해 계약·데이터모델·스펙 문서만 근거로 테스트 작성·실행) — 001과 동일한 역할 분리 패턴.

**검증 방법과 결과**:

- `npx tsc --noEmit`: 클린(0 오류) — 메인 에이전트가 직접 재실행해 확인.
- `npx eslint src`: 클린 — 메인 에이전트가 직접 재실행해 확인.
- `npx vitest run`: **24 test files / 129 tests 전부 통과**(001의 기존 65개 그대로 유지 + 002 신규 64개) — 메인 에이전트가 직접 재실행해 확인.
- 실제 저장소(`study-progress/` 8개 로드맵, `courses/` 7,865개 자료) 대상 메인 에이전트 독립 재실행(서브에이전트 보고와 별개로): `reload()` 2회 연속 — `roadmapCount=8, materialCount=7865, errorCount=0`, 두 실행 결과 완전 동일(SC-004). `listRoadmaps()` 100회 평균 응답시간/최초 적재시간 비율 = 0.00014(SC-001 기준 1% 대비 크게 여유). 캐시 파일 삭제 후 재적재, 그리고 손상시킨 뒤 재적재 — 두 경우 모두 삭제 전과 `listRoadmaps()` 결과 완전 동일(SC-003). `getRoadmapDetail()`/`listReviewNeededItems()` 정상 동작 확인(현재 실저장소 오류 0건이라 `listReviewNeededItems()`는 `[]` — QA의 별도 오류 픽스처 테스트(T026)로 100% 노출 여부는 따로 검증됨).
- SC-002(파일이 원본): 실제 `study-progress/` 파일을 고치는 것은 사용자의 실제 진도 데이터를 건드리므로 메인 에이전트가 직접 재현하지 않고, QA의 격리된 픽스처 통합 테스트(T014, 임시 디렉터리 기준 체크박스 수정 → 재적재 → 완료 수 정확히 +1)로 검증.
- SC-005(확인 필요 항목 100% 노출): QA가 체크박스 해석 불가·날짜 오류·링크 깨짐을 섞은 신규 픽스처로 `listReviewNeededItems()`가 001의 `runImport()` 오류 목록과 정확히 1:1 대응하는지 검증(T026).

**품질 검수(원칙 VI)에서 발견해 재작업으로 되돌린 실제 결함**: 없음. 다만 계획(`/speckit-plan`) 단계에서 메인 에이전트가 작성한 계약(`contracts/persistence-library.md`)에 FR-004가 요구하는 "경로" 검색 필터가 누락돼 있던 것을 `/speckit-tasks` 단계에서 발견해 태스크 생성 전에 수정했다(위 "스펙 문서 갱신" 참고) — 구현 결함이 아니라 계획 문서 자체의 결함이었다.

**구현 중 발견해 범위를 조정한 설계 결정** (자세한 근거는 `tasks.md`의 Revision note, `research.md` §3 참고):

- 원안은 재적재마다 `load_run_file_snapshots`로 `previousBatch`를 구성해 001 `resolveIdentity`에 주입하려 했으나, 001의 현재 계약(`RunImportOptions`/`parseRoadmaps`/`parseMaterials`)에는 `previousBatch`를 받을 매개변수가 없다는 것이 구현 중 확인됐다.
- 더 근본적으로, 001의 `resolveIdentity`가 반환하는 `status: "updated"`는 "내용이 실제로 바뀜"과 "내용이 그대로인 채 재적재됨"을 구분하지 못한다(001 자신의 문서 주석이 인정하는 한계) — 이 상태에서 무작정 `previousBatch`를 매 재적재마다 먹이면 `material_versions`가 변경 없이도 재적재마다 자료 수만큼(현재 실저장소 기준 7,865건) 무의미하게 불어나는 실제 버그가 발생한다는 것을 메인 에이전트가 코드 검토로 확인했다.
- 001은 이미 완료·배포된 기능이라 이번 기능 범위에서 확장하지 않기로 하고: 처음에 구현됐던(계산만 하고 버리는) 죽은 코드를 제거했고, `load_run_file_snapshots`는 "쓰기 전용 감사 기록"으로 목적을 재정의해 유지했다. FR-006/SC-004(재적재해도 중복 없음)는 이것과 무관하게 이미 성립한다 — 001의 식별자가 애초에 경로의 결정적 함수(`sha256(정규화 경로)`)이고, 매 재적재가 기존 파일에 덧쓰지 않고 완전히 새 파일을 만들기 때문이다(메인 에이전트가 실제 저장소로 2회 연속 재적재해 직접 확인).
- **알려진 후속 과제**: 파일 이동/이름 변경 시 식별자 연속성, 그리고 `material_versions`가 실제 변경 이력을 채우기 시작하려면 001의 `previousBatch` 매개변수 추가와 "내용이 실제로 바뀐 경우만 updated로 취급"하는 수정이 함께 필요하다. 그 필요가 실제로 생기면(예: 3단계 화면이 자료 버전 이력을 보여줘야 할 때) 별도 기능으로 다룬다.

**다음 단계**: 이 기능은 "조회 가능한 상태를 만든다"까지이며(spec.md Assumptions), 화면(로드맵 탐색, 자료실)이나 HTTP API는 만들지 않는다. 다음은 3단계(사용자·읽기 화면)에서 이 기능의 다섯 조회 함수(`contracts/persistence-library.md`)를 그대로 재사용하는 화면 구현이다.

---

## 2026-09-22 — 로드맵 탐색·자료실 화면 (Roadmap & Materials Screens)

**대응 스펙**: `specs/003-roadmap-materials-screens/`(spec.md, plan.md, research.md, data-model.md, contracts/http-routes.md, quickstart.md, tasks.md T001~T029 전부 완료)

**변경 범위**: `backend/src/web/` 신규 생성(서버 렌더링 단일 앱 — Express, 별도 프론트엔드 빌드 없음). 002의 `backend/src/persistence/queries.ts`에 함수 1개(`getMaterialById`) additive 추가. 이 저장소에서 **원칙 I(프론트엔드)이 처음 실질적으로 적용된 기능**이다.

- `backend/src/web/`: `types.ts`(백엔드·프론트엔드 공유 뷰 모델 계약, 메인 에이전트가 병렬 위임 전에 직접 작성), `html.ts`(이스케이프 헬퍼), `server.ts`(Express 앱 + 에러 처리 미들웨어), `continueStudy.ts`("이어서 공부" 순수 계산), `materialContent.ts`(자료 본문 읽기 + Markdown→안전한 HTML), `routes/{roadmaps,materials}.ts`(5개 라우트), `views/{layout,roadmapList,roadmapDetail,materialSearch,materialView}.ts`, `public/styles.css`(반응형 스타일시트)
- `backend/src/persistence/queries.ts`: `getMaterialById(materialId, dbPath?)` 추가(002 contracts Addendum)
- `backend/tests/unit/`: `continueStudy.test.ts`, `materialContent.test.ts`, `persistence.getMaterialById.test.ts` 신규 3개
- `backend/tests/integration/`: `web.roadmapList`, `web.roadmapDetail`, `web.continueStudy`, `web.materialSearch`, `web.materialView`, `web.routesContract` 신규 6개
- `backend/package.json`: `express`, `remark-rehype`, `rehype-raw`, `rehype-sanitize`, `rehype-stringify`(dependencies), `supertest`, `@types/express`, `@types/supertest`(devDependencies), `web` 스크립트 추가

**담당 서브에이전트**: 이번엔 역할이 셋으로 나뉘었다 — 백엔드 구현 1개(라우트·비즈니스 로직·002 확장), 프론트엔드 구현 1개(뷰 렌더링 함수·CSS, 백엔드와 병렬 진행 — 메인 에이전트가 미리 작성한 `types.ts`와 함수 시그니처 계약을 근거로 서로 기다리지 않고 동시 작업), QA 검증 1개(구현과 분리해 계약·데이터모델·스펙 문서만 근거로 테스트 작성·실행).

**검증 방법과 결과**:

- `npx tsc --noEmit` / `npx eslint src`: 클린 — 메인 에이전트가 직접 재실행해 확인.
- `npx vitest run`: **33 test files / 181 tests 전부 통과**(001/002의 기존 129개 유지 + 003 신규 52개) — 메인 에이전트가 직접 재실행해 확인.
- **실제 브라우저 검증(원칙 I/IV — 이 저장소에서 처음 요구된 절차)**: Playwright(격리된 스크래치 디렉터리에 임시 설치, 저장소 의존성에는 추가하지 않음)로 데스크톱(1440px)·모바일(390px) 두 뷰포트에서 골든 패스 전체를 직접 구동 — 로드맵 목록→상세→"이어서 공부"(302 리다이렉트로 미완료 항목까지 정확히 이동)→자료 검색→자료 본문 열람(실제 자료의 표·제목·링크가 정상 렌더링)까지 스크린샷과 함께 확인. 콘솔 오류 0건.
- 실제 저장소(로드맵 9개, 자료 7,865개) 기준 `GET /`, `GET /roadmaps/:id`, `GET /materials?category=articles` 등을 curl/Playwright로 직접 호출해 응답 확인.

**품질 검수(원칙 VI)에서 발견해 수정한 실제 결함** (자동 테스트가 아니라 실제 브라우저 확인 과정에서 발견됨 — 이번 기능에서 실제 브라우저 확인이 왜 필수인지를 보여주는 사례):

1. **모바일 뷰포트 가로 스크롤 버그**: 자료 검색 결과의 "연결된 로드맵" 링크가 64자 해시를 공백 없이 표시해, 390px 뷰포트에서 페이지 전체가 가로로 넘쳤다(`scrollWidth=579` vs `clientWidth=390`). `styles.css`의 `body`에 `overflow-wrap: anywhere` 한 줄을 추가해 수정하고, Playwright로 수정 전/후 스크린샷과 수치 재측정으로 확인(spec.md Edge Cases 요구 충족).
2. **캐시 미적재 시 사용자에게 스택 트레이스 노출**: 백엔드 구현 중 002 저장소가 아직 적재되지 않았을 때 라우트가 예외를 던지면 Express 기본 오류 페이지가 그대로 노출된다는 것을 백엔드 서브에이전트가 스스로 발견해 보고했다. 메인 에이전트가 `server.ts`에 에러 처리 미들웨어를 추가해 503 + 안내 메시지로 대체하고, 캐시 파일을 임시로 옮겨 직접 재현·재검증했다(spec.md Edge Cases "적재가 필요하다는 것을 알 수 있는 상태를 보인다").
3. (경미) `materialSearch.ts`의 "다음 N개 보기" 링크 href가 이스케이프를 거치지 않던 것을 발견해 수정 — 실질적 보안 위험은 없었으나(URL 값 자체는 이미 퍼센트 인코딩됨) 일관성을 위해 반영.

**구현 중 확정한 설계 결정**: 001의 `runImport()`가 Markdown 안의 raw HTML을 안전하게 "렌더링"해야 한다는 요구(F09)를 만족하려면 `rehype-sanitize`만으로는 부족하고(raw HTML이 애초에 파싱되지 않음) `rehype-raw`가 함께 필요하다는 것을 계획 단계에서 미리 발견해 `research.md`에 반영해 두었다 — 구현 중 재작업 없이 한 번에 반영됨.

**다음 단계**: 이 기능은 F02(로드맵 탐색)·F09(자료실)까지다. 다음은 5장 development 순서표의 4단계(브리핑)나 F03/F04(학습 핵심 루프) 중 우선순위를 정해 새 speckit 기능으로 이어간다.

---

## 2026-09-23 — 복습큐 자료 이전·영속화 (Review Queue Persistence)

**대응 스펙**: `specs/004-review-queue-persistence/`(spec.md, plan.md, research.md, data-model.md, contracts/review-queue-library.md, quickstart.md, tasks.md T001~T032 전부 완료)

**배경**: 원래 이 순서로 만들려던 것은 F01 브리핑(`specs/005-briefing/`)이었다. `/speckit-clarify` 과정에서 "브리핑이 재사용하려던 002의 `listReviewNeededItems`가 사실은 자료 파싱 오류 조회용이지 복습큐(간격 반복 다음 복습일)와는 무관한 별개 개념"이라는 것이 드러났고, 001·002 어느 것도 `내학습/`을 다룬 적이 없다는 것이 확인돼, 복습큐 파싱·영속화를 별도 선행 기능으로 분리하기로 확정했다(005-briefing spec.md Clarifications 참고). 그 결정에 따라 이 기능이 004 번호를 가져가고 브리핑은 005로 밀렸다.

**변경 범위**: `backend/src/reviewQueue/` 신규 생성(순수 파싱 모듈, 001의 AST 파싱 스타일 재사용). `backend/src/persistence/{schema,load,queries,types}.ts`(002·003) additive 확장 — 새 SQLite 파일을 만들지 않고 기존 캐시 파일에 테이블 3개를 추가했다(웹전환 명세 7.1 "SQLite 파일 하나" 확정 사항).

- `backend/src/reviewQueue/`: `types.ts`(`ActiveReviewItem`/`MasteredItem`/`ReviewImportError`), `dateParse.ts`(엄격한 `YYYY-MM-DD` + 실제 달력 유효성 검증, 예외 대신 판별 유니언 반환), `identity.ts`(NFC 정규화 후 `sha256(item+topic+firstWrongDate)` 결정적 id), `parseReviewQueue.ts`(unified/remark-parse/remark-gfm AST로 "마스터 완료" 헤딩 아래 표만 mastered, 나머지는 active로 구분 — 표 순서가 아니라 헤딩 기준)
- `backend/src/persistence/schema.ts`: `review_queue_items`/`mastered_items`/`review_import_errors` 테이블 추가, `SCHEMA_VERSION` 1→2(기존 "버전 불일치 시 전체 재생성" 경로 재사용, 별도 마이그레이션 없음)
- `backend/src/persistence/load.ts`: `loadReviewQueue()`(파일 없으면 빈 문자열)·`populateReviewQueue()` 추가, 002의 기존 원자적 재적재 패스 안에서 함께 적재(두 번째 reload 경로·두 번째 SQLite 파일 없음)
- `backend/src/persistence/queries.ts`: `getReviewQueueStatus(referenceDate?, dbPath?)`/`listMasteredItems(dbPath?)`/`listReviewQueueImportErrors(dbPath?)` 추가(002의 기존 다섯 함수 시그니처는 변경 없음)
- `backend/tests/`: 유닛 4개(`reviewQueue.dateParse`, `reviewQueue.identity`, `schema.reviewQueue`, `reviewQueue.parseReviewQueue`), 통합 3개(`persistence.reviewQueue.reload`, `.errors`, `.mastered`), 계약 정합성 1개(`contractConformance.reviewQueue`), 픽스처 5종(`normal`/`with-errors`/`empty`/`mastered-only`/`mixed.md`)

**담당 서브에이전트**: 백엔드 구현 1개(Setup·Foundational·US1~US3 구현 — 테스트 제외), QA 검증 1개(구현 소스 코드를 읽지 않고 spec·data-model·contracts·quickstart 문서만 근거로 테스트 작성·실행) — 001·002·003과 동일한 역할 분리 패턴.

**검증 방법과 결과**:

- `npx tsc --noEmit` / `npx eslint src`: 클린 — 메인 에이전트가 직접 재실행해 확인.
- `npx vitest run`: **41 test files / 219 tests 전부 통과**(001~003의 기존 181개 유지 + 004 신규 38개) — 메인 에이전트가 직접 재실행해 확인.
- 실제 저장소(`내학습/복습큐.md`, 활성 항목 10건, 마스터 완료 0건) 대상 메인 에이전트 독립 재실행: `reload()` 후 `getReviewQueueStatus('2026-09-23')` → `totalActiveCount=10`, 연체 일수 내림차순(5,5,5,4,4,0,0,0,0) 9건이 정확히 반환되고 내일(9-25) 예정 항목 1건만 제외됨(SC-001). `listMasteredItems()`/`listReviewQueueImportErrors()` 둘 다 `[]`(실제 파일에 해당 데이터가 없음과 일치).
- SC-002(재적재 무중복): QA의 격리된 임시 디렉터리 통합 테스트(같은 픽스처로 `reload()` 2회, `getReviewQueueStatus()` JSON 완전 동일·`review_queue_items` 행 수 불변)로 검증.
- SC-003(마스터 완료가 활성 조회에 안 섞임), SC-004(오류 행 분리·100% 노출), SC-005(빈 상태 오류 없음): QA가 `mixed.md`/`with-errors.md`/`empty.md` 픽스처로 검증.
- spec.md Edge Cases 전부 대조(T031): QA 테스트가 다루지 않은 두 가지 — ① 원본 파일이 아예 없는 경우, ② 같은 항목이 재적재 사이에 활성→마스터 완료로 이동하는 경우 — 는 메인 에이전트가 격리된 임시 디렉터리로 직접 재현해 확인(①은 `{totalActiveCount:0, dueItems:[]}`, ②는 이동 후 활성 조회에서 사라지고 마스터 완료 조회에만 나타나며 id는 그대로 유지됨).

**품질 검수(원칙 VI)에서 발견한 것**: 재작업으로 되돌린 실제 결함은 없음. QA가 스스로 "독립성 공개" — 참고 스타일 파일을 훑어보던 중 실수로 `reviewQueue/dateParse.ts`·`identity.ts`·`types.ts` 구현 내용까지 읽었다고 보고했다(원래는 `backend/src/persistence/types.ts`만 보기로 했었음). 두 함수 모두 규칙이 문서(research.md §2·§3)에 명확히 적혀 있어 이 노출이 거짓 통과를 만들었을 가능성은 낮다고 판단했지만, 메인 에이전트가 해당 두 테스트 파일과 실제 구현을 직접 대조해 이 세션 안에서 재검증했다(위 검증 항목 참고) — 완전히 새로운 제3자 재확인은 아니라는 한계는 기록해 둔다.

**구현 중 확정한 설계 결정** (자세한 근거는 `research.md` 참고):

- 새 SQLite 파일을 만들지 않고 002의 기존 캐시 파일에 테이블만 추가 — 001/002가 파싱·영속화를 분리한 이유는 "책임 분리"였지 "파일 분리"가 아니었고, 웹전환 명세 7.1이 이미 "SQLite 파일 하나"를 확정했기 때문.
- 항목 식별자는 `sha256(item+topic+firstWrongDate)` — 001의 "이번 실행 자체의 누적 원장을 자기 자신에게 먹이지 않는다"는 설계(실행 중 실제 버그를 낳았던 사례)를 참고해, 이전 배치 비교 없이 매 재적재를 상태 없는 전체 재계산으로 처리.
- `review_queue_items`/`mastered_items` 삽입에 `INSERT OR IGNORE` 사용(원안의 단순 `PRIMARY KEY` INSERT였다면 내용이 완전히 같은 두 행이 있을 때 재적재 전체가 예외로 죽는다 — data-model.md는 이 교차 재적재 유일성만 다루고 파일 내부 중복은 다루지 않아, "행 하나가 나머지 전체를 막지 않는다"는 이 기능의 일반 원칙(US2)에 맞춰 백엔드가 판단·문서화).
- `getReviewQueueStatus()`는 "오늘"을 스스로 계산하지 않고 항상 호출자(005-briefing)가 `referenceDate`를 명시적으로 넘긴다 — 시간대 판단을 이 기능이 대신 내리지 않기 위함.

**다음 단계**: 이 기능은 "조회 가능한 상태를 만든다"까지이며 화면·HTTP API는 없다. 다음은 `specs/005-briefing/`가 이 기능의 네 함수(`contracts/review-queue-library.md`)를 그대로 재사용해 F01 브리핑 화면·로직을 구현하는 것이다.

---

## 2026-09-23 — 브리핑 (Briefing)

**대응 스펙**: `specs/005-briefing/`(spec.md, plan.md, research.md, data-model.md, contracts/briefing-library.md, contracts/http-routes.md, quickstart.md, tasks.md T001~T035 전부 완료)

**변경 범위**: `backend/src/briefing/` 신규 생성(순수 조립 + 이 기능 전용 SQLite 영속화). `backend/src/web/`(003)에 `routes/briefing.ts`·`views/briefing.ts`·`views/briefingHistory.ts` 추가, `server.ts`에 라우터 등록 — 이 저장소에서 **원칙 I(프론트엔드)이 두 번째로 적용된 기능**이다. 002·004의 공개 조회 함수만 호출하고 그 구현은 건드리지 않았다.

- `backend/src/briefing/`: `types.ts`, `formatProgress.ts`(FR-004/FR-005 진행률 표시 규칙), `buildSnapshot.ts`(002·004 조회 결과 → 순수 조립), `schema.ts`·`db.ts`(전용 SQLite 파일, 002·004와 달리 스키마 버전 불일치를 삭제·재생성이 아니라 예외로 표면화), `store.ts`(트랜잭션 기반 삽입·조회), `logFile.ts`(브리핑로그.md에 append-only 기록), `service.ts`(`generateBriefing()` 오케스트레이션)
- `backend/src/web/`: `routes/briefing.ts`(`GET /briefing`, `POST /briefing/rerun`, `GET /briefing/history`, `GET /briefing/history/:id`), `views/briefing.ts`, `views/briefingHistory.ts`, `server.ts`(라우터 등록 + `express.urlencoded` 미들웨어 추가)
- `backend/tests/`: 유닛 6개(schema/formatProgress/buildSnapshot/store/contractConformance 등), 통합 5개(service/service.dedup/logFile/web.briefing/web.briefing.history)

**중요한 설계 이력**: 원래 이 순서로 만들려던 것은 이 기능이었으나(`/speckit-clarify` 이전), 그 과정에서 "브리핑이 재사용하려던 002의 조회 함수가 복습큐와 무관한 별개 개념"이라는 것이 드러나 복습큐 파싱·영속화를 004로 먼저 분리했다(004 개발결과 참고). 그 결정에 따라 이 기능이 005 번호를 받았다.

**담당 서브에이전트**: 백엔드 구현 1개(서비스·라우트, 테스트 제외), 프론트엔드 구현 1개(뷰 렌더링, 백엔드와 병렬 진행 — 메인 에이전트가 미리 작성한 `briefing/types.ts` 공유 계약을 근거로 서로 기다리지 않고 동시 작업, 003과 동일한 패턴), QA 검증 1개(구현 소스 로직을 읽지 않고 spec·data-model·contracts·research 문서만 근거로 테스트 작성·실행). 품질 검수에서 발견한 결함 수정을 위해 백엔드 서브에이전트를 2회 추가 호출.

**검증 방법과 결과**:

- `npx tsc --noEmit` / `npx eslint src tests`: 클린 — 메인 에이전트가 직접 재실행해 확인.
- `npx vitest run`: **51 test files / 293 tests 전부 통과**(001~004의 기존 219개 유지 + 005 신규 74개) — 메인 에이전트가 직접 재실행해 확인.
- **실제 브라우저 검증(원칙 I/IV)**: Playwright(스크래치 디렉터리에 임시 설치, 저장소 의존성에는 추가하지 않음, 003과 동일한 절차)로 데스크톱(1440px)·모바일(390px) 두 뷰포트에서 골든 패스 전체를 직접 구동 — 전체 브리핑 → 로드맵으로 좁혀 보기(진행률 표는 하나만, 복습 목록은 전체 유지·평균 재계산 확인) → 존재하지 않는 로드맵 404 → 다시 실행(303 리다이렉트로 새 기록 이동) → 기록 이력 목록·상세(고정된 값, "생성 당시 값" 안내)까지 스크린샷과 함께 확인. 가로 스크롤 없음, 콘솔 오류 0건(의도된 404 탐색 로그 제외).
- 실제 저장소(로드맵 9개, 복습 대상 10건) 기준 `getReviewQueueStatus`/`listRoadmaps` 실데이터로 검증: 연체 9건이 정확한 순서로, 미시작 로드맵 6개가 접혀서 표시됨을 확인.
- **브라우저 검증 후 정리**: 위 실제 브라우저 확인 과정에서 "다시 실행" 버튼을 눌러 실제 `내학습/브리핑로그.md`·`내학습/briefing-history.sqlite`에 테스트성 기록 4건이 실제로 쓰였다 — 검증 후 메인 에이전트가 `git checkout`으로 브리핑로그.md를 원래 커밋 상태로 복원하고 briefing-history.sqlite를 삭제해 사용자의 실제 데이터를 오염 없이 정리했다(정리 후 diff 0 확인).

**품질 검수(원칙 VI)에서 발견해 재작업으로 되돌린 실제 결함**:

1. **FR-009 미구현**: "복습큐 자체가 비어 있음"과 "항목은 있으나 오늘 대상이 없음"을 구분해야 하는데, `buildSnapshot()`이 004의 `totalActiveCount`를 스냅샷에 옮기지 않아 화면이 두 경우를 항상 "밀린 복습 없음"으로 동일하게 표시하고 있었다 — 메인 에이전트가 diff 검토 중 발견. 원인은 구현이 아니라 `data-model.md`/`contracts/briefing-library.md`가 애초에 이 필드를 빠뜨린 설계 결함이었다. 메인 에이전트가 문서를 먼저 고친 뒤 백엔드 서브에이전트에게 `types.ts`~`views/briefing.ts` 전 구간에 걸친 수정을 위임했고, 재검증(타입체크·린트·전체 테스트·수동 렌더링 비교)까지 확인했다.
2. **범위를 벗어나 만들어진 파일**: 백엔드 서브에이전트가 요청 범위를 벗어나 `design/screen-map.json`(캔버스 화면 ↔ 실제 구현 대조 레지스트리, 이전에 존재한 적 없음)과 `backend/scripts/check-design-sync.ts`(그 대조 스크립트), `package.json`의 관련 스크립트 항목을 임의로 새로 만들었다 — 내용 자체는 정확했지만(실제로 실행해 확인) 요청받지 않은 범위라 메인 에이전트가 일단 세 가지 변경을 모두 되돌렸다. 이후 사용자가 되돌린 내용을 보고 "살려서 추가해"라고 명시적으로 요청해, 메인 에이전트가 동일한 내용으로 세 파일을 다시 만들고 `npm run check:design-sync`로 캔버스 12개 화면 전부와 실제 구현이 일치함(오류 0건)을 재확인한 뒤 정식으로 포함시켰다 — 최종적으로는 유지된 변경이다.
3. **(경미, QA가 발견) 안전 공백**: `generateBriefing()`이 이 기능의 유일한 쓰기 진입점인데 유일하게 `dbPath`/`logPath` 재정의 수단이 없어, 향후 이 함수를 실수로 오버라이드 없이 호출하면 실제 `내학습/` 파일에 조용히 쓰는 위험이 있었다 — QA가 테스트 격리 중 발견(직접 겪음). 메인 에이전트가 직접 옵션 2개(`dbPath`, `logPath`)를 추가해 002·004와 동일한 관례로 맞췄다(계약 문서 반영, 재검증 완료).

**구현 중 확정한 설계 결정** (자세한 근거는 `research.md` 참고):

- 브리핑 스냅샷은 파일에서 파생되는 캐시가 아니라 그 자체가 유일한 역사적 사실이므로(생성 후 원본이 바뀌어도 값이 그대로여야 함, FR-014), 002·004의 공유 캐시(스키마 버전 불일치 시 전체 재생성)에 얹지 않고 별도 SQLite 파일(`내학습/briefing-history.sqlite`)에 보관 — 웹전환 명세 7.1의 "SQLite 파일 하나" 원칙에 대한 의도적 예외.
- "브리핑 실행" 중복 판정은 (기준 날짜, 시간대, 대상 범위) 자연 키로 — 같은 키 재조회(새로고침)는 기존 기록 재사용, 사용자의 명시적 "다시 실행"만 그 키를 무시하고 새 기록 생성.
- 평균 진행률은 화면에 표시 중인(스코프로 필터링된) 로드맵만 대상으로 계산 — 로드맵 하나만 보고 있을 때 화면 밖 로드맵 값이 섞인 숫자를 보여주지 않기 위함.
- 004(복습 데이터) 조회 실패는 부분 열화(로드맵 진행률만 표시, 스냅샷 미저장)로 처리하고, 002(로드맵) 실패는 003의 기존 503 오류 미들웨어에 그대로 위임.

**다음 단계**: 웹전환_기능명세_및_개발가이드.md 5장 순서표의 1~4단계(규칙 확정·자료 이전·읽기 화면·브리핑)가 모두 완료됐다. 다음은 5단계(학습 핵심 루프, F03/F04)로 이어가거나, F08(로드맵 생성)·F11(가져오기·내보내기) 중 우선순위를 정한다.

---

## 2026-09-23 — 학습 핵심 루프 (Study Core Loop)

**대응 스펙**: `specs/006-study-core-loop/`(spec.md, plan.md, research.md, data-model.md, contracts/ai-grading-contract.md·study-service-library.md·http-routes.md, quickstart.md, tasks.md T001~T049 전부 완료)

**의의**: 001~005는 전부 읽기 전용 화면·집계였다. 이 기능이 이 저장소에서 처음으로 (1) 실제 AI(Claude Code 헤드리스 모드)를 호출하고, (2) 여러 턴에 걸친 대화형 세션 상태를 유지하며, (3) 004가 읽기 전용으로만 다루던 `내학습/복습큐.md`에 처음으로 새 항목을 쓴다. F01(브리핑)·F02(로드맵)·F09(자료실)에 이어 F03(학습 시작)·F04(질문·답변·채점)까지 구현되어, "보여주기"만 하던 앱이 처음으로 "실제 공부"를 시킬 수 있게 됐다.

**변경 범위**: `backend/src/ai/`(AI 서브프로세스 호출 캡슐화, 신규), `backend/src/study/`(세션 상태 기계 + 전용 SQLite 영속화, 신규), `backend/src/persistence/queries.ts`·`db.ts`에 `appendReviewQueueItem`/`openExistingForWrite` additive 추가, `backend/src/web/`에 `routes/study.ts`·`views/studyNew.ts`·`views/studySession.ts` 추가.

- `backend/src/ai/`: `types.ts`, `claudeProcess.ts`(`execFile`로 `claude -p --json-schema` 호출, 90초 타임아웃, 모든 실패를 `retry_needed`로 수렴), `promptBuilder.ts`(학습 자료·사용자 답변을 데이터로 명시하는 시스템 프롬프트, 프롬프트 인젝션 방어), `grader.ts`(질문 생성·사전지식 판정·채점·힌트·설명 다섯 함수, `--json-schema` 강제와 별개로 서버가 재검증)
- `backend/src/study/`: `types.ts`, `schema.ts`·`db.ts`(전용 파일 `내학습/study-sessions.sqlite`, 005와 동일하게 스키마 버전 불일치 시 삭제·재생성 대신 예외), `sessionMachine.ts`(질문 단위 상태 기계, 순수 함수), `targetResolver.ts`(주제/자료/로드맵 이어하기/자동 제안 대상 해석), `store.ts`(append-only 답변 시도), `reviewQueueWriter.ts`(`내학습/복습큐.md` 활성 표에 새 행 append, AST 기반으로 정확한 위치에 splice), `service.ts`(전체 오케스트레이션)
- `backend/tests/`: 유닛 7개, 통합 6개(신규 74개 테스트)

**담당 서브에이전트**: 백엔드 구현 1개(AI 호출·세션 서비스·004 확장, 테스트 제외), 프론트엔드 구현 1개(화면, 백엔드와 병렬 진행 — 메인 에이전트가 미리 작성한 `ai/types.ts`·`study/types.ts` 공유 계약 근거), QA 검증 1개(구현 소스 로직을 읽지 않고 spec·data-model·contracts·research 문서만 근거로 테스트 작성·실행, `ai/grader.ts`를 모킹해 결정적으로 검증).

**검증 방법과 결과**:

- `npx tsc --noEmit` / `npx eslint src tests`: 클린 — 메인 에이전트가 직접 재실행해 확인.
- `npx vitest run`: **61 test files / 389 tests 전부 통과**(001~005의 기존 293개 유지 + 006 신규 74개, 아래 두 결함 수정 이후 재확인 포함) — 메인 에이전트가 직접 재실행해 확인.
- **실제 AI 호출 검증**: 메인 에이전트가 실제 `claude -p`로 학습 핵심 왕복을 직접 구동 — 사전지식 판정(hasMaterial:true 정확 판정)→AI가 생성한 인출 질문(OLTP/OLAP 설계 차이를 정확히 묻는 양질의 질문)→"모르겠습니다" 제출→AI가 생성한 힌트(정답을 직접 알려주지 않으면서 방향 제시, FR-024 취지 부합)까지 확인. 실측 지연 시간(9~17초)이 research.md의 사전 측정(15.7초)과 일치. 회당 비용 절약을 위해 5개 시나리오 전체를 실제 호출로 반복하지 않고, 힌트 3회·설명 전환·자료 경로·자동 제안은 QA의 모킹된 결정적 테스트로 검증.
- **실제 브라우저 검증(원칙 I/IV)**: Playwright로 데스크톱(1440px)·모바일(390px)에서 `/study/new` 가로 스크롤 없음·콘솔 오류 0건 확인. 세션 진행 자체는 실제 서버에 대한 curl 왕복으로 HTML 응답을 직접 검사해 확인(질문 표시, 답변 전 정답·힌트 미노출, 힌트 목록, 이력, 로드맵 이어하기 폼 정상 동작).
- **실제 파일 안전성**: 검증 과정에서 실수로 실제 `내학습/브리핑로그.md`(005의 자동 스냅샷 생성 경로를 건드린 것)와 `내학습/study-sessions.sqlite`에 테스트성 데이터가 남았던 것을 확인 즉시 원래 커밋 상태로 복원·삭제해 정리했다(diff 0 재확인).

**품질 검수(원칙 VI)에서 발견해 수정한 실제 결함**:

1. **`toSubmitAnswerResult()`의 `reviewItemRegistered` 하드코딩**(QA 발견): 이미 해결된 질문을 재구성할 때(FR-028 중복 제출 재전송, 또는 이미 종결된 질문 재조회) `reviewItemRegistered`를 항상 `true`로 가정하고 있었다 — 실제로는 파일 쓰기는 항상 성공하지만 004 라이브 캐시 쓰기(`appendReviewQueueItem`)는 캐시 미적재 등으로 실패할 수 있고, 그 경우 최초 응답은 정확히 `false`를 반환했는데도 재구성 시에는 그 사실이 사라졌다. 오늘 시점에 화면(`studySession.ts`)이 이 값을 소비하지 않아 사용자 영향은 없었지만, 향후 F10 학습 기록 등이 이 값을 신뢰하게 될 경우를 대비해 `study_questions`에 `review_item_registered` 컬럼을 추가해 실제 값을 저장·재사용하도록 메인 에이전트가 직접 수정(schema.ts·store.ts·service.ts·types.ts 4개 파일, 전체 재검증 완료).
2. **"로드맵 이어하기" 진입점이 실제로는 막혀 있었음**(QA 발견): `studyNew.ts`의 "로드맵 이어하기" 섹션이 로드맵 목록(`/`)으로의 단순 링크뿐이었고, 그 뒤 어디에도 `/study/start`로 되돌아오는 폼이 없어 User Story 4의 이 경로가 백엔드(`targetResolver.ts`)는 완전히 지원하는데도 UI로는 도달 불가능했다(우회로로 "무엇을 할지 찾기"의 자동 제안 화면을 통해서만 도달 가능했음). `GET /study/new`가 002 `listRoadmaps()`로 진행 중인 로드맵을 조회해 원클릭 시작 폼으로 나열하도록 메인 에이전트가 직접 수정, 실제 데이터(ADsP·네트워크 서비스·데브옵스 로드맵)로 정상 동작 확인.
3. **범위 밖 발견(수정 없음)**: 검수 중 이 세션이 006과 무관하게 사용자의 다른 세션(터미널의 실제 `/study`·`/브리핑` CLI 사용)이 만든 커밋 3개(`fbf38ae` 이후)를 발견했다 — 그 세션의 `git add -A && git commit` 습관이 이 세션의 미완성 웹 개발 작업까지 함께 커밋해 커밋 메시지("브리핑: 2026-09-23" 등)와 실제 diff 내용이 크게 어긋나는 상태가 됐다. 실제 학습 데이터 손상은 없음을 확인했고(체크박스·복습큐 내용이 진짜 사용자 학습과 일치), 이력을 재작성하지 않고 사용자에게 있는 그대로 보고했다.

**구현 중 확정한 설계 결정** (자세한 근거는 `research.md` 참고):

- AI 호출은 `@anthropic-ai/claude-agent-sdk`가 아니라 `claude` CLI(`execFile` 서브프로세스)로 — 이 기능에 필요한 것은 "프롬프트 보내고 스키마 검증된 JSON 받기"뿐이라 SDK의 세션·도구 관리 기능이 과함. 실제 라이브 호출로 `--json-schema`+`--output-format json`의 `structured_output` 필드가 스키마를 정확히 만족함을 사전에 확인한 뒤 설계.
- 매 턴(답변 제출·힌트·재시도)은 완결된 POST 요청 하나 — 채점 15~20초 지연 동안 웹소켓·SSE 같은 실시간 프로토콜을 도입하지 않고 001~005의 서버 렌더링 패턴을 그대로 유지.
- 복습큐 신규 등록은 004의 무거운 전체 `reload()`(실측 18초)를 매 오답마다 부르지 않고, 파일에 먼저 append한 뒤 004의 라이브 SQLite 캐시에 `appendReviewQueueItem()`(신규 additive 함수)으로 직접 반영 — 004 계약을 건드리지 않고 즉시 반영.
- 세션·질문·답변 데이터는 002/004/005의 어떤 캐시도 아닌 전용 SQLite 파일(`내학습/study-sessions.sqlite`)에 — 005가 이미 확립한 "파일에서 파생되지 않는 진짜 데이터는 002/004의 공유 캐시에 얹지 않는다"는 원칙을 그대로 따름.
- AI 호출은 `--restricted --permission-prompts none --strict-mcp-config`로 도구·MCP 접근을 완전히 차단 — 학습 자료·사용자 답변에 프롬프트 인젝션이 섞여도 실행할 도구 자체가 없는 구조적 방어(헌법 4.1).

**다음 단계**: 이 기능은 F03·F04의 핵심 왕복까지다. F05(정교화·청킹·인터리빙 세부 규칙), F06(간격 반복 복습 일정 계산 자체 — 이번엔 최초 등록의 "+1일"만 구현), F07(세션 마무리·세 줄 요약·학습 시간)이 후속 기능으로 남아 있다.
