# 구현 전 결정사항

`웹전환_기능명세_및_개발가이드.md` 7장 "구현 전에 확정할 정책" 표의 10개 항목. 전부 확정됨 — 상세 근거와 관련 절(F0x)은 7장 표와 해당 F0x 절 본문을 참고.

## 우선순위 높음 — 개발 착수와 직결

**1. AI 실행 방식** — Claude Code 헤드리스 모드(`claude -p`) 또는 Claude Agent SDK를 백엔드에서 서브프로세스·라이브러리로 호출한다. 인증은 기존 Claude Pro/Max 구독 로그인을 그대로 쓴다 — 별도 API 키·과금 없음(개인용 로컬 웹앱이라 가능). 모델은 특정 버전 고정보다 최신 모델을 자동으로 따라가는 설정을 우선 검토한다. 요청·응답은 로그 파일에 전량 보관.

**2. 로드맵 수정·삭제 정책** — 개인용 단일 사용자 앱이므로 "공유 로드맵 vs 개인 사본" 구분을 두지 않는다. 버전은 수정할 때마다 새로 남기되 개수 제한 없음, 삭제도 사용자 본인이 언제든 가능(별도 승인 절차 없음). 별도 백업 기능은 이 정책에서 다루지 않는다.

## 우선순위 중간 — 개별 기능 구현 시점에 맞춰 적용

**3. 복습 마지막 단계** — 5단계(1일→3일→7일→16일→35일) 그대로 유지. 망각곡선의 간격이 로그형으로 벌어지므로 단계를 줄이지 않는다. 35일 성공 후 `복습 완료`로 보관하고 활성 큐에서 제외.

**4. 같은 날 재정답** — 교정 성공으로 기록하되 복습 단계를 증가시키지 않는다.

**5. 인터리빙 대상** — 오늘 예정인 다른 주제 1~2개만 대상으로 한다. 연체 항목은 제외한다 — 이 앱은 공부를 돕는 도구지 스트릭(streak)형 서비스가 아니므로 연체를 억지로 밀어넣지 않는다. 없으면 생략.

**6. 브리핑 필터** — 브리핑에서 특정 로드맵을 선택해서 봐도 복습 목록은 필터링하지 않고 항상 전체를 보여준다. 정렬은 연체 오래된 순 유지.

**7. 평균 진행률 계산식** — 집계 가능한 로드맵별 비율의 산술평균, 분모 0(항목 없는 로드맵)은 제외.

**8. 항목 통과·채점 통계 기준** — 근거를 포함한 확인 답변, 산출물 항목은 결과물 검토까지 거쳐야 통과. 최초 판정과 힌트 이후 재시도는 분리해서 집계.

**9. 예시 데이터 vs 실제 기록 분리** — 기본 분리, 사용자가 가져오기 범위를 명시적으로 선택. 저장 위치를 완전히 분리할지 플래그로만 구분할지는 구현 단계에서 정한다.

**10. GitHub 연동 범위** — 자동 동기화(커밋·푸시)는 웹앱 기능에 포함하지 않는다. 필요하면 CLI·개발 툴에서 사용자가 직접 수행한다. 웹앱은 내보내기·백업까지만 제공한다.

**전 컨텍스트 마지막 답변**
구현전_결정사항.md 정리 — Q&A 주고받은 흔적 다 빼고, 확정된 10개 결정만 깔끔하게 남겼습니다.

새 컨텍스트로 넘길 정보 — 이 저장소 전용 메모리에 두 개 남겼습니다(다음에 이 프로젝트 폴더에서 대화 시작하면 자동으로 불러옵니다):

진행 상태 메모: 이번 세션에서 한 일(화면 7개 신규 제작, 사용 흐름 감사·수정, 로드맵 생성 흐름을 진행률 방식으로 교체) + 확정된 10개 정책 요약 + 다음 단계(백엔드 개발 착수)
캔버스 재게시 방법 메모: design/canvas/*.dc.html을 고친 뒤 실제 게시된 캔버스 링크에 반영하는 절차(appifact-doc JSON 구조, 검증 스크립트) — 이거 없으면 다음 세션에서 또 처음부터 이 방식을 다시 알아내야 해서 남겨뒀습니다.