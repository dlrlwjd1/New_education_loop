# 학습 시스템 웹 전환 — 기능 명세 및 개발 가이드

작성일: 2026-09-16  
문서 상태: 저장소의 현재 동작을 분석한 웹 전환 설계 초안. 웹 기능의 구현 완료를 뜻하지 않는다.

## 1. 목적과 적용 범위

사용자가 웹에서 현재 학습 상황을 확인하고, 공부할 내용을 선택하고, AI 질문에 직접 답하고, 오답을 간격에 맞춰 복습하며, 개인 목표에 맞는 로드맵을 만드는 서비스로 전환한다. 대화가 끝나거나 브라우저를 닫아도 다음 접속에서 학습을 이어갈 수 있어야 한다.

이 문서에서 **기존 규칙**은 저장소에 이미 명시된 행동이고, **웹 설계 제안**은 이를 웹에서 구현하기 위해 추가한 정책이다. 제안이나 미결정 항목을 현재 기능으로 오인하지 않는다. 프레임워크, DB 제품, AI 제공자, 배포 서비스는 아직 확정하지 않는다.

### 1.1 현재 기능과 웹 기능 대응

| 현재 기능·자료 | 웹에서 제공할 기능 | 구분 |
|---|---|---|
| `/브리핑` | 오늘의 복습 우선순위, 로드맵 진행률, 브리핑 기록 | 기존 기능 이전 |
| `/study <주제>` | 주제별 질문·답변·채점 | 기존 기능 이전 |
| `/study <로드맵·Phase·강의 파일>` | 자료와 연결된 순차 학습 | 기존 기능 이전 |
| 인자 없는 `/study` | 복습 또는 이어서 공부할 내용 제안 | 기존 기능 이전 |
| `/study`의 지식 부족 분기 | 짧은 설명을 받은 뒤 다시 답하기 | 기존 기능 이전 |
| 정교화·청킹·인터리빙 | 이유 설명, 개념 묶기, 다른 주제의 복습 문제 섞기 | 기존 기능 이전 |
| 복습큐와 간격 반복 | 오답 목록, 복습 세션, 다음 복습일 계산 | 기존 기능 이전 |
| Phase 문서의 체크박스 | 항목별 완료 상태, 로드맵·Phase 진도 | 기존 기능 이전 |
| `/roadmap` | 자료 입력부터 Phase 구성까지 로드맵 생성 (목표 직접 입력은 보조 경로) | 기존 기능 이전 · F08 참고 |
| 기존 로드맵을 목표에 맞게 수정하는 맥락 | 로드맵 수정·버전 관리 | 수정 역할은 기존 지침, 구체적 웹 절차는 제안 |
| `courses/`, 강의 자료 인덱스, 부록 | 자료 탐색·검색·열람 | 기존 자료의 웹 접근 기능 |
| 진행상황·브리핑로그 파일 | 학습 이력·브리핑 이력 | 기존 기능 이전 |
| 세션 종료 후 파일 저장, Git 커밋·푸시 | 서버 저장, 기록 내보내기, 백업 | 웹에서 대체할 방식 제안 |
| 개인 저장소로 데이터 구분 | 개인 작업공간, 다중 사용자 배포 시 인증·인가 | 웹 운영에 필요한 추가 기능 |
| `.claude/skills/`, `CLAUDE.md` | 서버의 학습 정책과 AI 실행 절차 | 개발·운영 내부 기능 |
| `.specify/`, `speckit-*` | 웹 개발 명세·계획·검증 작업 절차 | 학습자 화면과 분리하는 개발 도구 |

### 1.2 반드시 유지할 학습 원칙

- 사용자가 먼저 생각하고 답한다. 답변 전에 정답·힌트·풀이 예시를 먼저 보여주지 않는다.
- 한 번에 질문 하나, 개념 하나를 다룬다. 한 세션에서 새 주제를 4개 이상 열지 않는다.
- 사용자가 모르면 이를 입력할 수 있게 한다. 기초 지식이 없으면 짧게 설명한 뒤 다시 인출하게 한다.
- 사용자를 대신해 과제를 해결하거나 제출용 코드를 완성하는 서비스로 바꾸지 않는다.
- 맞은 답과 틀린 답을 구분하고, 틀린 답만 신규 복습 항목으로 등록한다.
- 학습 내용과 진도는 대화 기록에만 의존하지 않고 영속 저장한다.
- 각 로드맵은 독립된 과목이다. 인공지능융합공학부 로드맵을 다른 로드맵의 필수 선행 과정으로 강제하지 않는다.
- 무엇을 공부할지는 사용자가 결정한다. 브리핑이 학습을 임의로 시작하거나 로드맵을 변경하지 않는다.

## 2. 전체 이용 순서와 화면 구성

### 2.1 첫 이용

1. 개인 작업공간에 들어온다. 다중 사용자 서비스에서는 로그인으로 사용자를 식별한다.
2. 시간대를 확인한다. 초기 제안값은 `Asia/Seoul`이며 사용자 설정으로 보관한다.
3. 기존 로드맵과 자료를 탐색한다. 처음에는 이미 배운 과목으로 시작하도록 안내한다.
4. 실제 학습 기록을 가져오거나 새 기록으로 시작한다. 예시 데이터 체험은 별도 공간에서 제공한다.
5. 브리핑에서 상황을 확인한 뒤, 사용자가 학습할 주제나 로드맵을 선택한다.

### 2.2 반복 이용

1. 브리핑을 실행해 오늘 복습 대상과 진행 상황을 확인한다.
2. 복습, 특정 주제 학습, 로드맵 이어하기 중 하나를 선택한다.
3. 질문에 답하고 피드백을 받는다.
4. 필요한 설명·재답변·정교화·다른 주제 복습을 진행한다.
5. 오늘 배운 것을 자료 없이 세 줄로 정리한다.
6. 저장 결과와 다음 복습일을 확인하고 종료한다.
7. 다음 접속에서는 저장된 진도와 복습큐를 읽어 이어간다.

### 2.3 주요 화면

| 화면 | 주요 구성 | 대표 행동 |
|---|---|---|
| 홈·브리핑 | 기준 날짜, 복습 우선순위, 로드맵별 진행률 | 브리핑 실행, 공부할 내용 선택 |
| 로드맵 목록·상세 | 목표, 트랙, Phase, 항목, 참고 자료·부록 | 이어서 공부, 특정 Phase 선택 |
| 학습실 | 현재 질문, 답변 입력, 채점 결과, 단계 표시 | 답변 제출, 모름 제출, 일시 중단 |
| 복습큐 | 항목, 관련 주제, 복습일, 반복 단계 | 오늘 복습 시작, 항목 이력 보기 |
| 자료실 | 자료 분류, 검색, 본문, 출처 링크 | 읽기, 자료 기반 학습 시작 |
| 로드맵 만들기·수정 | 자료 입력(기본)·목표 입력(보조), 자동 생성 진행률, 지식·Phase·자료 연결 결과 | 생성 시작, 완성 후 확인·저장, 필요시 수정 |
| 학습 기록 | 세션별 학습 내용, 정답·오답, 사용자 요약 | 기록 열람, 재개, 내보내기 |
| 브리핑 기록 | 당시 복습 수·진도 스냅샷 | 날짜별 결과 확인 |
| 설정·데이터 관리 | 시간대, 가져오기, 내보내기 | 이전 결과 확인, 백업 다운로드 |

화면 경로와 메뉴 이름은 구현 단계에서 결정한다. 핵심 동작은 위 표를 기준으로 유지한다.

## 3. 기능별 상세 동작

### F01. 브리핑 — 오늘의 상태를 모아 보기

**기존 근거:** `.claude/skills/브리핑/SKILL.md`.

**사용 단계**

1. 사용자가 전체 또는 특정 로드맵을 대상으로 브리핑을 실행한다.
2. 서버가 사용자 시간대의 오늘 날짜를 확정한다.
3. 대상 로드맵의 모든 하위 Phase에서 완료·미완료 항목을 집계한다.
4. 복습큐에서 다음 복습일이 오늘이거나 지난 항목을 추린다.
5. 복습일이 오래 지난 항목부터 보여준다.
6. 로드맵별 `완료/전체`와 진행률을 표시한다.
7. 해당 시점의 결과를 브리핑 기록에 저장한다.

**표시·계산 규칙**

- `완료 == 0`, `전체 > 0`: `0% (미시작)`과 `0/전체`를 표시한다.
- 완료 항목이 있으면 소수 첫째 자리까지 표시한다. 반올림 시 `0.0%`가 되면 `<0.1% (진행 중)`으로 표시한다.
- 예: `5/799 → 0.6%`, `5/251 → 2.0%`. 항상 원래 분수를 함께 보여준다.
- 미완료가 남았는데 반올림으로 `100.0%`가 되는 경우에는 `<100%`로 표시한다는 웹 정책을 제안한다. 완료 판정은 분수로 한다.
- Phase가 없으면 `진행 자료 없음`, Phase는 있으나 집계 가능한 항목이 없으면 `형식 확인 필요`로 구분한다. `0/0`은 0%로 처리하지 않는다.
- 미시작 로드맵이 4개를 넘으면 이름 중심으로 접어 보여주되 각 상세 화면으로 이동할 수 있게 한다.
- 복습큐 자체가 비어 있으면 `아직 쌓인 오답이 없습니다`, 항목은 있으나 오늘 대상이 없으면 `밀린 복습 없음`으로 구분한다.
- 날짜가 잘못된 항목은 별도 표시하고, 정상 항목의 브리핑은 계속한다.

**개발 시 주의점**

- 브리핑은 진도와 복습큐를 읽기만 한다. 완료 처리나 복습 일정 변경을 수행하지 않는다.
- 기존 스킬은 특정 로드맵 선택 시 복습큐까지 제한할지 명확하지 않다. **확정(7장)**: 필터링하지 않는다 — 브리핑에서 특정 로드맵을 선택해서 보더라도 복습 목록은 항상 로드맵 구분 없이 전체를 보여준다. 복습 항목은 여전히 연체가 오래된 순으로 정렬한다(변경 없음).
- 단순 화면 조회와 기록 생성 요청을 구분한다. 화면 새로고침만으로 동일한 로그가 계속 쌓이지 않게 한다.
- 명시적인 브리핑 재실행은 새 기록이며, 동일 실행 요청의 재전송은 기존 결과를 반환한다.
- 집계 수치와 기록은 동일한 시점의 데이터를 사용한다. 이후 진도가 바뀌어도 과거 브리핑 숫자는 바뀌지 않는다.
- 평균 진행률은 기존 정의가 불명확하다. 웹 제안은 집계 가능한 로드맵별 비율의 산술평균이며, 미시작은 포함하고 `0/0`은 제외한다. 대상 수와 계산 방식을 기록한다.

**완료 확인:** 복습 기한 경계, 소수 진행률, 빈 로드맵을 정확히 표시하고 기록은 실행당 한 건 생성된다.

### F02. 로드맵 탐색과 학습 위치 선택

**기존 근거:** `study-progress/`, 각 로드맵 README·Phase·강의 자료 인덱스·부록.

**사용 단계**

1. 로드맵 목록에서 목표와 현재 진행률을 확인한다.
2. 로드맵을 열어 트랙, Phase, 소주제, 학습 항목을 탐색한다.
3. 관련 강의 자료와 부록, 도착점, 예상 분량을 확인한다.
4. `이어서 공부`를 선택하면 첫 미완료 항목을 제안한다.
5. 특정 Phase 또는 강의 항목을 선택해 해당 위치에서 시작할 수도 있다.

**개발 시 주의점**

- 현재 로드맵은 8개지만 개수를 코드에 고정하지 않는다.
- 인공지능융합공학부 로드맵처럼 트랙 아래에 Phase가 있는 구조를 지원한다. 직속 파일만 읽으면 누락된다.
- 트랙·Phase·항목의 순서는 원문에서 보존해 명시적 순서 값으로 저장한다. 제목의 문자열 정렬에만 의존하지 않는다.
- 부록과 인덱스도 열람 가능해야 한다. 현재 브리핑의 집계 대상은 파일명에 `Phase`가 있는 문서의 체크박스이며, 부록 체크박스를 몰래 분모에 추가하지 않는다.
- 같은 강의가 여러 로드맵에 연결되어도 각 학습 항목의 완료 상태는 독립적으로 유지한다.
- 단순 열람이나 `알겠어요` 클릭으로 완료 처리하지 않는다. 완료 판정은 F04의 검증 절차와 연결한다.

**완료 확인:** 중첩된 트랙과 부록까지 접근 가능하며, 첫 미완료 항목과 진행률이 원본과 일치한다.

### F03. 학습 시작 — 주제·자료·자동 제안

**기존 근거:** `/study`의 0절, A 경로, B 경로.

**사용 단계**

1. 사용자가 `주제 공부`, `로드맵 이어하기`, `자료로 공부`, `무엇을 할지 찾기` 중 하나를 선택한다.
2. 주제 공부에서는 주제명을 입력하고, 자료 기반 학습에서는 실제 로드맵·Phase·자료를 선택한다.
3. 입력이 없으면 오늘 복습 대상, 진행 중 로드맵, 로드맵 후보 2~3개 순으로 제안한다.
4. 사용자가 대상을 확정하면 세션을 생성한다.
5. 주제 경로는 자료 설명 전에 `지금 아는 것을 적어 주세요`로 시작한다.
6. 자료 경로는 서버가 자료를 먼저 읽고, 사용자에게 제목과 함께 사전 지식을 묻는다.

**개발 시 주의점**

- 자연어 입력이 주제명인지 로드맵명인지 애매하면 두 선택지를 보여준다. 모델이 조용히 경로를 확정하지 않는다.
- 자료 경로는 등록된 자료 ID로 처리한다. 브라우저가 보낸 임의의 서버 파일 경로를 열지 않는다.
- 첫 질문과 함께 자료 요약이나 정답을 미리 내려보내지 않는다.
- 자료가 없거나 열리지 않으면 오류를 알리고 다른 자료 선택 또는 주제 모드 전환을 사용자가 선택하게 한다.
- 웹 제안: 오늘 복습 대상으로 연체 항목도 포함한다. 기존 인자 없는 `/study`의 `오늘 복습일` 표현을 브리핑의 `오늘 또는 지난 날짜`와 일치시킨다.

**완료 확인:** 모든 시작 경로가 동작하고, 인자 없는 시작에서도 사용자가 대상을 선택한 후 학습이 시작된다.

### F04. 질문·답변·채점과 지식 부족 처리

**기존 근거:** `/study` 2~3절, A-4~A-5.

**사용 단계**

1. 질문 하나를 표시하고 답변을 기다린다.
2. 사용자가 답변을 제출하거나 `모르겠습니다`를 제출한다.
3. 서버가 답변을 저장한 뒤 채점을 요청한다.
4. `맞음`, `유창하지만 틀림`, `모름`으로 판정하고 맞은 부분·틀린 부분을 구분해 보여준다.
5. 근거까지 맞으면 정교화 질문으로 이어간다.
6. 틀린 부분은 사용자 답변의 표현을 인용해 짚고 복습 대상으로 기록한다.
7. 기초 지식이 있지만 인출에 막힌 경우 답변 이후 힌트 하나를 제공하고 다시 답하게 한다.
8. 동일 항목에서 힌트를 세 번 제공해도 답하지 못하면 기초 설명으로 전환한다.
9. 사전 지식 확인에서 처음부터 재료가 없다고 판정되면 힌트 세 번을 강제로 거치지 않고 바로 설명으로 전환한다.
10. 설명은 5분 분량 이하, 부품 개념 3~5개로 제한한다. 설명 패널을 닫고 다시 자기 말로 답하게 한다.

**개발 시 주의점**

- 힌트 제공 횟수는 항목별로 서버에 저장한다. 새로고침으로 초기화하지 않는다.
- 채점 실패·시간 초과는 `모름`이나 오답이 아니다. `채점 대기/재시도 가능` 상태로 남긴다.
- AI가 반환한 문장을 그대로 DB 갱신 명령으로 사용하지 않는다. 판정 형식과 허용된 상태 전이를 서버가 검증한다.
- 근거 자료가 충돌하거나 부족하면 판정을 보류한다. 잘못된 자동 채점의 재검토 경로를 제공한다는 웹 정책을 제안한다.
- 답변 전에는 정답 필드를 응답 JSON·HTML·숨겨진 UI에 포함하지 않는다. CSS로 가리는 것만으로 구현하지 않는다.
- 사용자 코드 작성은 입력·제출 기능으로 지원할 수 있다. 서버에서 임의 코드를 실행하는 기능은 별도 실행 격리 설계 없이는 추가하지 않는다.
- 기존 지침에는 항목 통과의 수치 기준이 없다. 웹 제안은 핵심 확인 질문에 근거를 포함해 답하고, 산출물 항목은 결과물 검토까지 통과했을 때 완료하는 것이다. 단순 세션 종료를 전체 항목 완료로 처리하지 않는다.
- 정답·오답 통계에서 힌트 후 재답변을 어떻게 집계할지 정한다. 제안은 최초 채점 결과와 이후 시도 수를 분리 저장하고, UI에 집계 기준을 명시하는 방식이다.

**완료 확인:** 먼저 답하는 순서, 모름 분기, 힌트 제한, 짧은 설명 후 재인출, 오류 시 채점 보류가 모두 작동한다.

### F05. 정교화·청킹·인터리빙

**기존 근거:** `/study` 4~5절.

**사용 단계**

1. 기본 개념에 근거를 포함해 답하면 `왜 이렇게 설계했는가`와 같은 이유 질문을 한다.
2. 사용자의 설명을 확인한 뒤 관련 개념들에 하나의 이름을 붙이게 한다.
3. 세션 중간이나 끝에 복습큐에서 현재 주제와 다른 항목 1~2개를 섞어 낸다.
4. 해당 답변도 동일하게 채점하고 그 복습 항목의 이력에 반영한다.

**개발 시 주의점**

- 기초 지식이 없는 사용자에게 이유 질문을 계속하지 않는다.
- 개념 묶기의 이름과 설명은 사용자가 작성한다. AI가 먼저 완성하지 않는다.
- 인터리빙을 이유로 여러 질문을 한 화면에서 한꺼번에 제출하게 하지 않는다.
- 기존 스킬에는 인터리빙 항목의 복습일 조건이 명확하지 않다. **확정(7장)**: 오늘 예정인 다른 주제 항목만 대상으로 하고, 연체된 항목은 인터리빙 대상에서 제외한다 — 이 앱은 공부를 돕는 도구이지 "매일 빠짐없이 채우는" 스트릭(streak)형 서비스가 아니므로, 연체를 억지로 밀어넣지 않는다. 오늘 예정 항목이 없으면 생략한다.
- 미래 복습 항목을 연습하도록 확장할 경우 정규 간격을 이동시킬지 별도 정책이 필요하다. 기본 구현에서는 조기 복습을 자동 편성하지 않는다.

**완료 확인:** 기본 지식 확인 후 심화가 시작되고, 다른 주제 복습 결과가 해당 항목에만 반영된다.

### F06. 복습큐와 간격 반복

**기존 근거:** `/study` 6절, `내학습/복습큐.md`.

**사용 단계**

1. 최초로 틀린 항목을 사용자 복습큐에 등록한다.
2. 최초 오답일, 주제·로드맵, 다음 복습일, 반복 단계를 저장한다.
3. 다음 복습일이 오늘이거나 지났으면 복습 대상으로 표시한다.
4. 복습에서 맞히면 다음 간격으로 이동한다.
5. 틀리거나 인출하지 못하면 1일 간격으로 되돌린다.
6. 변경된 다음 복습일과 복습 이력을 확인한다.

**일정 규칙**

| 사건 | 다음 일정 |
|---|---|
| 최초 오답 | 오답 발생일 + 1일 |
| 1일 단계 복습 성공 | 실제 복습일 + 3일 |
| 3일 단계 복습 성공 | 실제 복습일 + 7일 |
| 7일 단계 복습 성공 | 실제 복습일 + 16일 |
| 16일 단계 복습 성공 | 실제 복습일 + 35일 |
| 어느 단계든 복습 실패 | 실제 복습일 + 1일, 첫 단계로 복귀 |
| 35일 단계 복습 성공 | 기존 정의 없음. 아래 제안 참조 |

`실제 복습일`을 기준으로 재계산하는 것은 웹 설계 제안이다. 연체된 원래 기한에 간격을 더해 곧바로 다시 연체 상태가 되는 것을 방지한다. **확정(7장)**: 5단계(1일→3일→7일→16일→35일)를 그대로 유지한다 — 망각곡선의 간격이 로그(대수)형으로 벌어지는 것을 반영하려면 중간 단계를 줄이면 안 된다는 이유다. 35일 단계 성공 후에는 `복습 완료`로 보관하고 활성 큐에서 제외한다.

**개발 시 주의점**

- 날짜는 사용자 시간대의 달력 날짜로 계산한다. 실행 시각은 별도로 UTC 시각으로 저장한다.
- 동일 항목에 대한 반복 요청이나 같은 세션 내 교정 답변으로 간격이 여러 단계 증가하면 안 된다.
- 최초 오답을 곧바로 고쳐 답해도 다음 날 복습은 유지한다는 정책을 제안한다. 설명 직후의 성공을 간격 복습 성공으로 취급하지 않는다.
- 복습 항목 ID와 개념 ID를 둔다. 표시 문장만 비교해 중복을 판정하지 않는다. 의미가 비슷하다는 이유만으로 별개 항목을 자동 합치지 않는다.
- 신규 정답은 큐에 넣지 않지만 기존 오답의 성공 복습 기록은 남겨야 한다.
- 날짜 오류를 오늘로 임의 변환하지 않는다. 수정이 필요한 항목으로 분리한다.
- 시간대를 바꿔도 기존 날짜 필드를 임의로 재작성하지 않는다. 변경 이후의 오늘 판정과 새 일정 계산에 적용하는 것을 제안한다.

**완료 확인:** 성공·실패·연체·동일 요청 재전송·날짜 변경 상황에서 복습 단계와 다음 날짜가 정확하다.

### F07. 세션 마무리·진도 저장·중단 후 재개

**기존 근거:** `/study` A-5, 6~7절, `내학습/진행상황.md`.

**사용 단계**

1. 사용자가 세션 마무리를 선택한다.
2. 오늘 배운 내용을 자료 없이 세 줄로 작성한다.
3. 자료 기반 학습이면 통과한 항목의 완료 상태와 날짜를 반영한다.
4. 정답·오답 집계, 주제, 로드맵·Phase, 사용자 요약, 학습 시간을 저장한다.
5. 신규 오답과 복습 결과를 반영하고 저장 완료를 표시한다.
6. 일시 중단한 경우 다음 접속에서 마지막 질문·답변 상태를 불러와 이어간다.

**개발 시 주의점**

- 기존은 세션 종료 시 파일 갱신이다. 웹에서는 제출한 답변과 확정된 채점을 중간 저장하고, 종료 시 요약·완료 상태를 확정하는 것을 제안한다.
- 브라우저 종료 이벤트에만 저장을 의존하지 않는다. 입력 초안과 서버에 제출된 답변을 구분한다.
- 학습 항목 완료는 해당 항목의 평가 통과에 근거해야 한다. 주제 모드가 비슷한 제목의 로드맵을 자동으로 체크해서는 안 된다.
- 요약을 작성하지 않고 나가면 `중단됨/마무리 대기`로 남긴다. AI가 사용자 요약을 대신 작성하지 않는다.
- 진도·복습 상태·채점 반영은 트랜잭션과 처리 식별자로 일관성을 보장한다. 종료 단계에서 이미 반영한 복습을 다시 적용하지 않는다.
- 시간은 긴 비활성 구간을 제외한 활동 시간으로 계산하는 것을 제안한다. 체류 시간을 곧 학습 시간이라고 표시하지 않는다.
- 과거 완료 날짜가 없는 가져오기 항목은 날짜 미상으로 보존한다. 가져온 날을 학습한 날로 만들지 않는다.

**완료 확인:** 새로고침, 다른 기기 접속, 종료 중 저장 실패에서도 확정된 답변과 진도가 유실·중복되지 않는다.

### F08. 개인 로드맵 생성·수정

**기존 근거:** `.claude/skills/roadmap/SKILL.md`, `CLAUDE.md`의 로드맵 변경 역할.

**개정 이력:** 최초 초안은 목표 직접 입력을 1단계로 뒀다. 이후 `design/canvas/Roadmap.dc.html` 디자인 시안에서 **자료(파일·URL) 입력을 기본 경로**로 바꾸고 목표 직접 입력을 보조 경로로 내렸으며, 논의 후 이 문서를 그 흐름에 맞춰 갱신했다 — 디자인이 아니라 명세 쪽의 정책 변경이다.

**두 번째 개정:** 최초 갱신판은 "필요 지식 제안 → 사용자 검토 → Phase 구성 → 자료 연결 → 최종 검토"를 사용자가 화면마다 멈춰서 승인하는 절차로 뒀다. 디자인 시안에서 이 중간 승인 화면들을 만들지 않고 **진행률(%) 표시 하나로 뭉쳐 AI가 이어서 처리**하도록 바꿨고, 결과가 마음에 들지 않을 때의 교정 경로는 이미 있는 **F08 자체의 "수정" 절차**(10단계, `RoadmapEdit`)로 합의했다 — 생성 중간에 멈춰서 검토하는 화면을 별도로 만들지 않는다. 아래는 그 흐름을 반영한 순서다.

**사용 단계**

1. 사용자가 자료(강의 페이지, 문서, 파일, 사내 위키 등 URL·업로드)를 입력한다. 자료가 없거나 원치 않으면 "자료 없이 목표만 적을게요"로 전환해 완주 후 만들 수 있어야 하는 결과물을 한 문장으로 적는 보조 경로를 선택한다.
2. 자료 경로에서는 서버가 자료를 읽어 학습 포인트를 정리해 보여준다. 목표 경로에서는 이미 아는 것, 처음인 것, 주당 학습 시간, 목표 기한을 입력한다.
3. 사용자가 "로드맵 만들기 시작"으로 확정하면, AI가 다음을 이어서 처리한다: 필요한 지식 목록 제안(자료 경로는 정리된 학습 포인트 기반, 목표 경로는 목표 달성에 필요한 지식 기반) → 선행 관계에 따른 Phase 구성과 각 Phase의 도착점 설정 → 기존 로드맵 연결 재사용 또는 전체 자료 목록·검색을 활용한 자료 연결(자료 경로에서 이미 입력한 자료는 자동으로 연결 후보에 포함) → 적합한 자료가 없는 항목에는 사용자가 직접 만들 산출물 과제 제안. 이 구간에서는 화면마다 멈춰 세워 개별 승인을 받지 않고, 진행률만 보여준다.
4. 처리가 끝나면 완성된 로드맵(목표, Phase, 학습 항목, 연결된 자료, 진행 상황 0%)을 보여주고 저장한다. 이 시점의 저장을 최종 확정으로 본다 — 저장 전 단계별 승인 화면을 별도로 두지 않는다.
5. 생성된 로드맵은 일반 로드맵과 같은 방식으로 학습·브리핑에 연결된다.
6. 결과가 마음에 들지 않으면 생성 과정을 다시 열지 않고, 10단계의 로드맵 수정 절차(변경 내용 먼저 보여주기 → 유지·추가·삭제 확인 → 새 버전 저장)로 고친다. 즉 **"확인하며 만들기"가 아니라 "만들고 나서 고치기"** 가 이 명세의 정책이다.
7. 수정 시 변경 내용을 먼저 보여주고, 유지·추가·삭제되는 항목과 진도에 미칠 영향을 확인한 후 새 버전으로 저장한다.

**개발 시 주의점**

- 순서는 `자료 입력(기본) 또는 목표 입력(보조) → (진행률만 노출되는) 지식 제안·Phase 구성·자료 연결 자동 처리 → 완성된 로드맵 저장 → 필요하면 수정 절차로 교정`이다. 두 입력 경로는 1~2단계만 다르고 이후 단계는 동일하다.
- 자료 정리(요약·추출)와 그 이후 지식 목록·Phase·자료 연결까지는 AI가 이어서 처리하고 사용자는 진행률만 본다. **결과를 사용자에게 처음 보여주는 시점은 완성 후이며, 그 결과에 대한 교정은 F08의 수정 절차(6~7단계)로 한다** — 생성 도중의 별도 검토·승인 화면을 만들지 않는다.
- 자료에서 뽑은 내용을 검증 없이 그대로 저장하지 않는다는 원칙은 유지한다. 다만 그 검증은 사용자가 매 화면마다 승인하는 방식이 아니라, 완성된 결과물 저장 시점의 서버 검증(자료 ID 존재 확인 등)과 이후의 수정 절차로 이뤄진다.
- URL 자료는 등록되지 않은 임의 URL이라도 이 단계에서는 입력을 허용하되, 실제 스크래핑 전에 접근 가능 여부·크기 제한을 확인하고 실패 시 오류를 사용자에게 알린다. F03의 "브라우저가 보낸 임의의 서버 파일 경로를 열지 않는다"는 자료실에 이미 등록된 자료를 여는 경우의 제약이며, 로드맵 생성 단계의 신규 자료 입력에는 별도의 입력 검증(허용 프로토콜, 크기·시간 제한)을 적용한다.
- 기존 스킬의 20~40개 지식 항목, 4~8개 Phase, Phase당 5~15시간은 일반적인 가이드로 사용한다. 모든 목표에 강제하는 입력 제한으로 만들지 않는다.
- 자료 입력 단계는 저장해 중간 이탈 후 재개할 수 있게 한다. 자료 입력 중 이탈해도 이미 추가된 자료 목록은 보존한다. 반면 3~4단계(AI 자동 처리 구간)는 진행 중 이탈 시 처음부터 다시 시작하는지, 이어서 완료 알림을 받는지는 구현 전 확정한다.
- 자료 연결은 실제 자료 ID와 존재 여부로 검증한다. AI가 만든 가짜 경로를 저장하지 않는다.
- 자료를 못 찾았다는 판단 전에 전체 카탈로그와 기존 매핑을 확인한다. 검색 결과 상위 몇 개만 보고 자료가 없다고 단정하지 않는다.
- 수정해도 유지되는 항목은 기존 ID와 진도를 보존한다. 삭제한 항목의 학습 이력은 기록에서 사라지지 않게 한다.
- **확정(7장)**: 개인용 단일 사용자 앱이므로 "공유 기본 로드맵 vs 개인 사본"을 구분하지 않는다. 수정은 그 로드맵의 새 버전으로 남기면 되고, 다른 사용자 진도를 고려한 격리 로직은 필요 없다. 버전 개수 제한이나 삭제 승인 절차도 두지 않는다.
- 삭제·브리핑 제외는 기존 동작이 상세히 정의되지 않았다. 추가한다면 제외·보관·삭제를 구분하고 대상과 영향을 사용자에게 확인한다.

**완료 확인:** 자료 입력·목표 입력 두 경로 모두 동일한 자동 처리 구간(진행률 표시)으로 합류해 완성된 로드맵을 내놓고, 결과가 마음에 들지 않을 때 생성 과정 재진입 없이 수정 절차만으로 교정되며, 없는 자료의 산출물 대체와 수정 전후 진도 보존이 동작한다.

### F09. 자료실·검색·본문 열람

**기존 근거:** `courses/`, 로드맵의 강의 자료 인덱스·부록.

**사용 단계**

1. 제공처·강좌·주제·제목으로 자료를 찾는다.
2. 검색 결과에서 제목, 경로 계층, 관련 로드맵을 확인한다.
3. 본문, 코드 블록, 표, 내부 링크, 원본 출처 링크를 열람한다.
4. 선택한 자료로 학습을 시작하거나 로드맵의 해당 위치로 이동한다.

**개발 시 주의점**

- 조사 시점에 `courses/`에는 Markdown 7,865개가 있다. 요청마다 전체 폴더를 순회하거나 전체 본문을 브라우저로 전송하지 않는다. 색인과 페이지 단위 조회를 사용한다.
- 분류는 현재 `articles`, `deeplearning-ai`, `mooc`, `udemy`, `youtube`이며 추가 분류를 수용한다.
- 수집된 정리본에는 전사·요약 품질 차이가 있다. 출처와 관련 주의 문구를 보존하고, 불충분한 자료를 확정적인 채점 근거로 쓰지 않는다.
- 학습자는 원본 자료를 직접 덮어쓰지 않는다. 개인 메모가 필요하면 별도 데이터로 저장한다.
- Markdown의 raw HTML과 링크를 안전하게 렌더링한다. 스크립트와 위험한 URL을 실행하지 않는다.
- 상대 경로, 공백, 한글, URL 인코딩, 중첩 디렉터리, 앵커를 실제 사례로 검증한다. 경로 문자열을 단순 분할하는 파서를 피한다.
- 파일명 정규화 차이를 처리하되 정규화 충돌은 탐지한다. 표시 제목과 실제 자료 식별자는 분리한다.
- 일반 자료 열람과 인출 시험은 구분한다. 학습 중 자료를 열었다면 `자료 참고` 시도로 기록하는 웹 정책을 제안한다. 외부에서 자료를 봤는지까지 보장하는 시험 감독 기능은 아니다.

**완료 확인:** 실제 저장소 자료의 표·링크·코드가 정상 표시되고, 검색 후 자료 기반 학습으로 연결된다.

### F10. 학습 이력·브리핑 이력

**기존 근거:** `내학습/진행상황.md`, `내학습/브리핑로그.md`.

**사용 단계**

1. 날짜, 로드맵, 주제로 학습 기록을 조회한다.
2. 세션 상세에서 질문·답변·판정, 사용자 요약, 완료 항목, 복습 일정 변경을 확인한다.
3. 중단된 세션은 이어서 진행한다.
4. 브리핑 이력에서 당시 복습 대상 수와 로드맵별 진행률을 확인한다.

**개발 시 주의점**

- 현재 수치와 당시 스냅샷을 구분한다. 과거 로그를 현재 값으로 다시 계산해 덮어쓰지 않는다.
- 기존 기록은 요약 표이므로 질문·답변 원문이 없다. 가져오기 시 없는 상세 데이터를 AI로 만들어 채우지 않는다.
- 기존 브리핑의 `평균 진행률` 열에는 텍스트와 개별 로드맵 비율이 섞여 있다. 원문을 보존하고 파싱 가능한 수치만 구조화한다.
- 정답 수는 평가 기준과 시도 방식에 영향을 받는다. 이를 객관적인 숙련도 점수처럼 이름 붙이지 않는다.
- 기록의 날짜와 수정 시각, 가져온 시각은 별도 필드로 관리한다.

**완료 확인:** 새로 로그인해도 기록이 남고, 이전한 요약 기록과 웹에서 생성한 상세 기록을 구분해 열람할 수 있다.

### F11. 기존 데이터 가져오기·내보내기·백업

**기존 근거:** Markdown 파일 저장과 Git 커밋·푸시. 다음 절차는 웹 대체 방식 제안이다.

**사용 단계**

1. 등록된 저장소 자료 또는 허용된 업로드에서 가져올 내용을 선택한다.
2. 로드맵·자료·개인 진도·복습큐·로그를 분리 분석한다.
3. 생성·갱신·보류될 항목, 깨진 링크, 날짜 오류, 중복을 미리 보여준다.
4. 사용자가 가져올 범위를 확정하면 트랜잭션 단위로 반영한다.
5. 이전 결과와 원본 비교 보고서를 확인한다.
6. 필요할 때 개인 로드맵·진도·복습큐·기록을 Markdown 및 구조화된 데이터로 내보낸다.
7. 운영 백업은 별도로 수행하고 실제 복구를 검증한다.

**개발 시 주의점**

- `사용과개선.md`는 현재 학습 기록이 과제용 가상 데이터라고 명시한다. 개인 학습 파일과 연결된 Phase 완료 체크를 실제 신규 사용자의 진도로 자동 배정하지 않는다.
- 예시는 별도 체험 작업공간에 넣는다. 정규 사용자 초기 상태는 미시작·빈 복습큐를 기본으로 제안한다.
- 자료·로드맵 구조와 개인 완료 체크를 분리해서 가져온다. 동일한 기본 로드맵을 사용해도 개인 진도는 다르다.
- 최초 이전에서만 파일을 기준으로 상태를 옮기고, 웹 운영 이후에는 DB를 개인 진도의 원본으로 삼는 방식을 제안한다. 파일·DB 양쪽을 동시에 원본으로 두지 않는다.
- 내보내기는 기존 원문과 구조를 보존한 사본으로 만든다. 가져오기 매핑과 안정적인 ID를 포함해 재이전 시 중복을 방지한다.
- Markdown 코드 블록 속 예시 체크박스는 학습 항목으로 세지 않는다. 실제 목록 구조를 파싱하고 기존 집계와 차이가 있으면 보고한다.
- `[x]`, `[X]`, 공백 변형은 처리하되 해석할 수 없는 항목은 보류한다. 문서 전체를 조용히 누락하지 않는다.
- 원본 파일 경로·내용 해시·가져오기 배치를 저장해 같은 자료를 다시 가져올 때 중복 생성하지 않는다.
- 저장소 전체를 사용자 파일로 노출하지 않는다. 개인 기록·콘텐츠·개발 설정·비밀정보를 구분한다.
- 웹 기록 보관을 위해 매 요청마다 서버에서 Git 커밋·푸시를 실행하지 않는다. **확정(7장)**: 자동 Git 동기화는 웹앱 범위에 아예 포함하지 않는다 — 필요하면 사용자가 CLI·개발 툴에서 직접 커밋·푸시한다.

**완료 확인:** 가져오기 전후 항목 수와 완료 수가 대조되고, 두 번째 가져오기에서 중복이 생기지 않으며 내보낸 기록으로 복구할 수 있다.

### F12. 개인 작업공간·설정·운영

**웹 전환에 필요한 지원 기능 제안이다. 현재 저장소에 로그인 기능이 구현되어 있다는 뜻은 아니다.**

**사용 단계**

1. 사용자가 자신의 작업공간에 접근한다.
2. 시간대와 기본 학습 설정을 확인한다.
3. 학습 및 기록 조회는 자신의 데이터 범위에서 수행한다.
4. 운영자는 자료 가져오기 실패, AI 오류, 저장 실패를 확인하고 복구한다.

**개발 시 주의점**

- 개인용 로컬 웹앱과 여러 사용자가 접속하는 서비스 중 배포 형태를 먼저 확정한다. 다중 사용자 배포에서는 인증과 서버 측 소유권 검사가 필수다.
- 다른 사용자의 ID를 요청에 넣어도 세션·복습큐·로드맵 사본·내보내기 파일에 접근할 수 없어야 한다.
- AI 제공자 키와 서버 비밀정보는 브라우저에 전달하지 않는다.
- 운영 로그에는 요청 ID·오류 종류·소요 시간·정책 버전을 우선 남긴다. 개인 답변 전문이나 비밀정보를 무조건 기록하지 않는다.
- 호출 횟수·입력 크기·사용자별 동시 세션을 제한하고, 한도를 넘거나 AI가 중단되면 입력과 현재 상태를 보존한다.
- 복구 작업과 자료 버전 갱신이 기존 학습 기록을 덮어쓰지 않도록 한다.
- 휴대전화 화면, 키보드 조작, 입력창 포커스, 색상 외의 정오답 표현을 확인한다. 스트리밍 출력이 답변 입력을 방해하지 않아야 한다.

**완료 확인:** 사용자 데이터가 분리되고, AI 또는 저장 장애가 발생해도 제출된 답변을 복구할 수 있다.

## 4. 서버가 보장해야 하는 공통 설계

### 4.1 AI와 애플리케이션의 책임 분리

| AI가 수행할 일 | 서버가 확정할 일 |
|---|---|
| 질문·힌트·짧은 설명 생성 | 현재 단계, 다음에 허용되는 행동 |
| 근거를 포함한 채점 제안 | 판정 형식 검증, 보류·재시도, 결과 저장 |
| 정교화 질문과 피드백 생성 | 항목별 힌트 횟수, 신규 주제 수 제한 |
| 목표에서 필요한 지식 제안 | 로드맵 저장, 자료 ID 존재 확인 |
| 자료 연결 후보 제안 | 진도·복습 일정·통계 계산 |

진행률, 복습 날짜, 권한 검사를 AI의 자연어 계산에 맡기지 않는다. 저장소의 스킬 Markdown을 프롬프트로 넣는 것만으로 웹 기능이 구현되지는 않는다. 핵심 절차를 서버 상태와 검증 가능한 규칙으로 옮겨야 한다.

강의 본문과 사용자 답변은 학습 자료다. 그 안의 `이전 규칙을 무시하라`, 파일 삭제 명령 등을 시스템 지시로 실행하지 않는다. 모델에 DB·셸을 직접 수정할 권한을 주지 않는다.

### 4.2 학습 상태 전이

기본 흐름은 다음과 같다. 상태명은 구현 예시다.

```text
대상 선택 → 사전 인출 대기 → 답변 저장 → 채점 대기
  ├─ 지식 부족 → 짧은 설명 → 재인출 대기
  ├─ 인출 실패 → 힌트 제공 → 재답변 대기
  ├─ 오답 → 교정 피드백·오답 기록 → 확인 질문
  └─ 정답 → 정교화 → 개념 묶기
                ↓
        다른 주제 복습(대상이 있을 때)
                ↓
        사용자 세 줄 요약 → 종료 확정

모든 대기 단계: 중단·재개 가능
외부 호출 실패: 현재 입력 보존 → 재시도 또는 종료 보류
```

단계 변경은 서버가 처리한다. 클라이언트가 임의로 `완료`를 전송했다고 그대로 받아들이지 않는다. 로드맵 생성도 `자료/목표 입력 → 자동 생성(진행률만 노출) → 완성본 저장` 단계를 따로 관리한다 — 자동 생성 구간은 사용자 승인 없이 서버가 이어서 처리하고, 결과 교정은 F08의 수정 단계(별도 상태)로 한다.

### 4.3 데이터 모델의 최소 구성

| 데이터 | 주요 내용 |
|---|---|
| 사용자·작업공간 | 소유자, 시간대, 데모 여부 |
| 자료·자료 버전 | 제목, 원본 경로, 본문, 출처, 내용 해시 |
| 로드맵·버전 | 목표, 소유 범위, 기간, 출발점, 버전 |
| 트랙·Phase·학습 항목 | 계층, 순서, 도착점, 강의 또는 산출물, 안정적인 ID |
| 개인 진도 | 사용자·항목별 상태, 완료 시각, 근거 세션 |
| 학습 세션 | 경로, 대상, 현재 단계, 중단 상태, 사용자 요약 |
| 질문·답변·채점 시도 | 질문 ID, 답변, 판정, 근거, 힌트 횟수, 시도 순서 |
| 복습 항목·복습 사건 | 개념, 최초 오답일, 단계, 복습일, 성공·실패 이력 |
| 브리핑 스냅샷 | 기준 날짜·시간대, 범위, 당시 집계, 요청 식별자 |
| 가져오기 배치·매핑 | 원본과 ID 연결, 오류 목록, 반영 상태 |
| 학습 정책 버전 | 프롬프트·채점·복습 정책 버전 |

개인 진도에는 사용자와 학습 항목 조합의 유일성을 보장한다. 답변 제출과 복습 적용에는 요청 식별자 또는 사건 식별자로 중복 반영을 막는다. 원본 자료가 변경되어도 이전 채점에서 참조한 자료 버전을 추적할 수 있어야 한다.

### 4.4 API 계약과 동시성

- 요청·응답 스키마, 권한, 성공·실패 상태, 재시도 가능 여부를 구현 전에 합의한다.
- 주요 명령은 세션 시작, 답변 제출, 힌트 제공, 세션 종료, 브리핑 생성, 로드맵 초안 저장, 가져오기 확정이다.
- 예: 답변 제출에는 `session_id`, `question_id`, `answer`, `request_id`, `expected_version`을 포함하는 계약을 제안한다.
- 같은 요청의 재전송은 같은 결과를 반환하고, 이미 다른 답변으로 진행된 세션에는 충돌을 알려 최신 상태를 조회하게 한다.
- AI 호출은 긴 DB 트랜잭션 밖에서 수행한다. 답변·작업 ID를 먼저 저장하고 결과를 받을 때 단계와 버전을 확인해 한 번만 반영한다.
- 스트리밍 중간 문장을 확정된 채점으로 저장하지 않는다. 검증된 완성 결과를 기준으로 상태를 전환한다.
- 진행률 캐시는 개인 진도나 로드맵 버전 변경 시 갱신한다. 다른 사용자 캐시가 섞이지 않게 한다.

## 5. 단계별 개발 순서

| 단계 | 구현 범위 | 다음 단계로 넘어갈 기준 |
|---|---|---|
| 1. 규칙·계약 확정 | 배포 형태, 기능 범위, 미결정 정책, 데이터 모델, API 계약 | 기존 규칙과 제안의 차이가 명시됨 |
| 2. 자료 이전 기반 | Markdown 파서, ID 매핑, 중첩 로드맵, 자료 색인, 예시 데이터 분리 | 원본과 가져오기 결과 수치·링크 대조 통과 |
| 3. 사용자·읽기 화면 | 작업공간, 필요한 인증·인가, 로드맵 탐색, 자료실 | 실제 자료 접근과 사용자 격리 확인 |
| 4. 브리핑 | 복습 대상 조회, 진행률 계산, 로그 스냅샷 | 날짜·진행률 경계 및 중복 요청 검증 |
| 5. 학습 핵심 루프 | 주제·자료 경로, 사전 인출, 질문·답변·채점, 모름·힌트·설명 | 답변 전 정답 미노출 및 중단·재개 검증 |
| 6. 학습 완결 | 정교화·청킹·인터리빙, 복습 일정, 완료 체크, 사용자 요약, 이력 | 첫 학습부터 다음 복습까지 전체 흐름 통과 |
| 7. 로드맵 작성 | 자료/목표 입력, 자동 생성(진행률), 완성 후 검토·수정·버전 | 생성한 로드맵으로 학습과 브리핑 가능 |
| 8. 데이터·운영 완성 | 내보내기, 백업·복구, 오류 복구, 접근성·모바일, 비용 제어 | 실제 브라우저 확인과 복원 시나리오 통과 |

1~6단계는 핵심 학습 기능을 갖춘 첫 배포 범위로 묶을 수 있다. 사용자가 요청한 전체 기능의 웹 전환 완료는 7~8단계까지 확인한 상태를 뜻한다.

### 개발 역할과 검수

현재 `.specify/memory/constitution.md`는 웹앱 구현에서 프론트엔드·백엔드·DBA·QA·디버거 역할을 전담 서브에이전트에 위임하고, 메인 에이전트가 실제 변경 내용을 검수하도록 규정한다. 실제 구현 계획은 그 역할 분리와 계약 공유 절차를 따라 작성한다. 이 문서는 요구사항·계약 정리 문서이며 구현 코드나 테스트 실행 결과를 대신하지 않는다.

- 프론트엔드: 화면, 입력 흐름, 접근성, 중단·재개 상태 표시.
- 백엔드: 학습 상태 전이, 인증·인가, AI 호출, 복습 계산, 중복 처리.
- DBA: 스키마, 마이그레이션, 유일성·참조 제약, 이전 및 복구.
- QA: 규칙 검증, 통합 시나리오, 실제 브라우저 확인.
- 디버거: 재현 조건과 근본 원인 분석 후 담당 역할에 수정 전달.
- 메인 에이전트: 계약 일치, 변경 범위, 실제 diff와 검증 증거 확인.

## 6. 출시 전 확인 시나리오

| 번호 | 시나리오 | 기대 결과 |
|---|---|---|
| T01 | 기록이 없는 신규 사용자 | 미시작·빈 복습큐 안내, 예시 진도 미반영 |
| T02 | 트랙 안에 Phase가 있는 로드맵 | 하위 항목이 누락 없이 탐색·집계됨 |
| T03 | 완료 0개, 5/799, 극소 비율, 0/0 | 미시작·소수 진행·진행 중·자료 이상 구분 |
| T04 | 오늘·어제·내일 복습 항목 | 오늘과 어제만 브리핑에 표시, 연체 우선 |
| T05 | 자정 전후·시간대 변경 | 사용자 달력 날짜로 일관되게 처리 |
| T06 | 주제/로드맵 이름이 애매한 입력 | 경로 선택을 요청하고 임의 시작하지 않음 |
| T07 | 첫 질문의 네트워크 응답 검사 | 답변 전 정답·힌트·풀이 필드가 없음 |
| T08 | 사전 인출에서 모름, 이후 힌트 세 번 실패 | 각 상황에 맞게 설명 후 재인출로 전환 |
| T09 | 서로 다른 주제의 복습을 섞음 | 해당 복습 항목에만 결과 적용 |
| T10 | 동일 답변을 두 번 제출하거나 두 탭에서 진행 | 한 번 반영 또는 충돌 안내, 이중 채점 없음 |
| T11 | AI 시간 초과·응답 형식 오류 | 답변 보존, 오답 처리 없음, 안전한 재시도 |
| T12 | 오답 직후 교정, 다음 날 성공, 이후 실패 | 당일 간격 건너뛰기 없음, 단계 증가 후 1일로 복귀 |
| T13 | 종료 중 통신 실패 후 재접속 | 확정 기록 유지, 동일 종료 요청 중복 반영 없음 |
| T14 | 자료만 열거나 요약 없이 종료 | 항목 자동 완료 없음, 적절한 중단 상태 유지 |
| T15 | 같은 자료가 두 로드맵에 연결됨 | 한쪽 완료가 다른 쪽 진도를 자동 변경하지 않음 |
| T16 | 로드맵 수정·자료 버전 변경 | 유지 항목 진도와 과거 평가 근거 보존 |
| T17 | 깨진 날짜·링크·체크박스·인코딩 | 오류 위치를 보고하고 정상 자료는 처리 |
| T18 | 다른 사용자의 세션·내보내기 ID 요청 | 서버에서 접근 거부 |
| T19 | 악성 Markdown·자료 속 지시문 | 스크립트 실행·시스템 명령 수행 없음 |
| T20 | 동일 데이터 재이전 및 백업 복원 | 중복 없음, 원본 수치와 복구 수치 일치 |
| T21 | 키보드·모바일로 학습 한 세션 완료 | 답변·피드백·저장 상태를 정상 이용 |
| T22 | 로그인 → 브리핑 → 학습 → 오답 → 재접속 → 복습 | 핵심 이용 순서 전체가 실제 브라우저에서 연결됨 |

실제 AI의 채점 품질은 정답·오답·모름·부분 정답·근거 부족 사례로 별도 평가한다. 고정된 가짜 AI 응답으로 통과한 테스트만으로 실제 채점 품질을 보장하지 않는다. 반대로 날짜 계산·권한·중복 반영 검증은 AI 결과에 의존하지 않는 결정적인 테스트로 확인한다.

## 7. 구현 전에 확정할 정책

문서 작성은 아래 결정 없이도 가능하지만, 관련 기능 구현 전에는 계약에 반영해야 한다. 선택 전까지 위 제안을 확정 요구사항으로 취급하지 않는다.

| 결정 항목 | 이 문서의 제안 또는 확인할 선택지 |
|---|---|
| 배포 형태 | **확정**: 개인용 로컬 웹앱. 다중 사용자 배포·계정 서비스는 이번 범위에서 제외한다. |
| 로드맵 생성 입력 방식 | **확정**: 자료(파일·URL) 입력이 기본 경로, 목표 직접 입력은 보조 경로 — `design/canvas/Roadmap.dc.html` 시안과 동기화 (F08 참고). |
| 로드맵 생성 중 검증 방식 | **확정**: 지식 제안·Phase 구성·자료 연결은 화면별 승인 없이 AI가 이어서 처리하고 진행률만 보여준다. 결과 교정은 생성 도중이 아니라 완성 후 로드맵 수정 절차(F08 6~7단계, `RoadmapEdit`)로 한다. |
| 데이터 원본 | **확정**: 진행 기록의 원본은 계속 파일(Markdown)이다. 별도 DB 서버는 두지 않고, 로컬 SQLite 파일 하나를 파일에서 파생되는 보조 저장소로만 둔다 — 아래 7.1 참고. |
| AI 실행 방식 | **확정**: 별도 Anthropic API 키·과금 없이, **Claude Code 헤드리스 모드**(`claude -p` 등 비대화형 호출) 또는 그 기반인 **Claude Agent SDK**를 서버 백엔드에서 서브프로세스·라이브러리로 호출한다. 인증은 이미 로그인된 사용자의 Claude Pro/Max 구독을 그대로 쓴다. 개인용 로컬 웹앱이라 가능한 구조이며(배포형 다중 사용자 서비스였다면 별도 API 키가 필요했을 것), 예산은 곧 "기존 구독 한도" 그대로다. 특정 모델 ID를 고정하기보다 최신 모델을 자동으로 따라가는 설정을 우선 검토한다. 요청·응답 원문은 로그 파일에 전량 보관한다(개인 단일 사용자라 데이터량 문제 없음). |
| 복습 마지막 단계 | **확정**: 5단계(1일→3일→7일→16일→35일) 그대로 유지한다. 망각곡선의 간격이 로그형으로 벌어지므로 단계를 줄이지 않는다. 35일 성공 후 `복습 완료`로 보관, 활성 큐에서 제외. |
| 같은 날 재정답 | **확정**: 교정 성공으로 기록하되 복습 단계를 증가시키지 않음 |
| 인터리빙 대상 | **확정**: 오늘 예정인 다른 주제 1~2개만 대상으로 하고, 연체 항목은 제외한다(스트릭형 기능이 아님). 없으면 생략. |
| 브리핑 필터 | **확정**: 로드맵을 선택해서 봐도 복습 목록은 필터링하지 않고 항상 전체를 보여준다(별도 "전체 보기" 전환 UI 불필요). 정렬은 기존대로 연체 오래된 순 유지. |
| 평균 진행률 | **확정**: 유효한 로드맵별 비율의 산술평균, 분모 0 제외 |
| 항목 통과·채점 통계 | **확정**: 근거 있는 확인 답변·산출물 검토, 최초 판정과 재시도 분리 |
| 로드맵 수정·삭제 | **확정**: 개인용 단일 사용자 앱이므로 "공유 로드맵 vs 개인 사본" 구분은 두지 않는다. 버전은 수정할 때마다 새로 남기되 개수 제한 없음, 삭제는 사용자 본인이 언제든 할 수 있다(별도 관리자 승인 불필요). 별도 백업 기능은 이 정책에서 다루지 않는다(F11의 일반 내보내기로 충분). |
| 예시·실제 기록 | **확정**: 기본 분리, 사용자가 가져오기 범위를 명시적으로 선택 |
| GitHub 연동 | **확정**: 자동 동기화(커밋·푸시)는 웹앱 기능에 포함하지 않는다. 필요하면 항상 CLI·개발 툴에서 사용자가 직접 수행한다. 웹앱은 내보내기·백업까지만 제공한다. |

### 7.1 로컬 배포에서 파일과 DB의 관계

로컬 단일 사용자 앱이므로 별도로 설치·구동하는 DB 서버(PostgreSQL 등)는 두지 않는다. 대신 원본과 보조 저장소의 역할을 이렇게 나눈다.

- **원본(파일, 그대로 유지)**: `study-progress/*/Phase*.md`의 체크박스, `내학습/진행상황.md`, `내학습/복습큐.md`, `내학습/브리핑로그.md`. 사람이 읽고 고치고 git으로 관리하는 대상은 계속 이 파일들이다. 웹 전환 이후에도 화면에서 조작한 결과는 이 파일에 반영되는 것을 기준으로 삼는다.
- **보조 저장소(SQLite 파일 하나)**: 파일만으로는 안전하게 보장하기 어려운 것만 SQLite로 옮긴다.
  - 답변 제출·복습 반영의 중복 방지 (`request_id` 유일성 — 4.3·4.4절)
  - 오늘 날짜 기준 복습 대상, 로드맵별 진행률 같은 빠른 조회·집계
  - 자료 버전과 과거 채점 결과의 참조 관계 추적
  - SQLite 파일(`내학습/learning-loop.db` 등)은 파일에서 파생되는 캐시·인덱스로 취급하고, 필요하면 파일을 다시 읽어 재생성할 수 있어야 한다. 즉 git에는 커밋하지 않아도 되는 대상이다 (`.gitignore` 처리).
- **동기화 방향**: 세션 시작 시 파일을 읽어 SQLite에 반영하고, 학습·복습 진행 중 발생하는 이벤트는 SQLite에 먼저 기록한 뒤 세션 종료(또는 주기적 시점)에 파일(체크박스, 복습큐, 로그)에 다시 써낸다. 파일이 SQLite보다 더 최신이면(예: 사용자가 Phase 문서를 직접 고친 경우) 파일 쪽을 우선한다.

## 8. 저장소 개발 도구의 위치

현재 `speckit-*` 스킬은 학습자 기능이 아니라 이 웹앱을 개발하기 위한 도구다. `모든 기능의 웹 전환`은 위 학습·자료·기록 기능을 대상으로 잡고, 개발 도구는 제품 화면 밖의 작업 절차로 유지하는 것을 제안한다. 개발 도구 자체를 웹 관리 콘솔로 제공하는 것은 별도 범위다.

| 개발 도구 | 역할 |
|---|---|
| `speckit-constitution` | 개발 원칙 정의·개정 |
| `speckit-specify`, `speckit-clarify` | 기능 명세 작성·모호한 요구사항 확인 |
| `speckit-plan`, `speckit-tasks` | 설계 계획·의존 관계에 따른 작업 분해 |
| `speckit-checklist`, `speckit-analyze` | 요구사항 점검·문서 간 일관성 분석 |
| `speckit-implement` | 작업 목록에 따른 구현 |
| `speckit-converge` | 실제 구현과 명세를 비교하고 미구현 작업 정리 |
| `speckit-taskstoissues` | 작업을 GitHub 이슈로 변환 |

스킬 파일을 수정할 수 있는 권한이나 개발 명령 실행 권한을 일반 학습자에게 노출하지 않는다. 학습 정책을 운영 중 변경한다면 검토·버전 관리·회귀 확인을 거쳐 적용한다.

## 9. 분석에 사용한 원본

- [CLAUDE.md](CLAUDE.md): 학습 원칙, 독립 과목 모델, 세 스킬의 역할.
- [README.md](README.md): 배포본 사용 흐름과 폴더 구조. 브리핑 추가 이전 설명이 있으므로 현재 기능 전체의 단독 기준으로 사용하지 않는다.
- [study 스킬](.claude/skills/study/SKILL.md): 주제·자료 경로, 인출, 채점, 복습, 기록.
- [roadmap 스킬](.claude/skills/roadmap/SKILL.md): 목표부터 자료 매칭까지의 순서.
- [브리핑 스킬](.claude/skills/브리핑/SKILL.md): 진도 집계, 복습 우선순위, 로그.
- [과제소개.md](과제소개.md): 브리핑 기능의 목적과 기존 시스템 확장 범위.
- [사용과개선.md](사용과개선.md): 가상 데이터 고지와 진행률 표시 개선 사례.
- [진행상황](내학습/진행상황.md), [복습큐](내학습/복습큐.md), [브리핑로그](내학습/브리핑로그.md): 현재 기록 형식.
- [웹앱 개발 헌법](.specify/memory/constitution.md): 역할별 구현·검증 원칙.
- `study-progress/`, `courses/`: 실제 중첩 구조, 자료 링크, 체크박스·완료 날짜 형태.
