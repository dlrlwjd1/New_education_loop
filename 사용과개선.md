# 사용과개선 — `/브리핑` 스킬

가상 데이터(과제 제출 규칙에 따라 실제 개인 학습 대신 예시로 구성한 진행 기록)로 두 번 실행하고,
그 사이에 발견한 문제 하나를 고쳐서 개선 전후를 비교한 기록이다.

## 사용 기록 1 — 2026-09-16, `/브리핑`

**상황**: 2026-09-14, 2026-09-15 이틀간 `/study`로 네트워크 서비스 로드맵과 데브옵스 로드맵을
각각 진행했다고 가정했다(`내학습/진행상황.md` 첫 두 줄). 두 세션 모두 오답이 나와
`내학습/복습큐.md`에 3건이 쌓인 상태에서 첫 `/브리핑`을 돌렸다.

**기대한 결과**: 오늘(2026-09-16) 기준으로 밀린 복습 항목이 마감 지난 순으로 보이고, 로드맵 8개의
진행률이 한 화면에 보인다.

**실제 결과**:

- 복습 우선순위: TCP 3-way handshake(1일 밀림), NAT 지점(1일 밀림) → DevOps 파이프라인 5단계 순서(오늘 마감).
  세 항목 모두 "오늘이거나 지남" 조건을 만족해 전부 떴다. 이어진 `/study` 세션은 인터리빙 규칙(1~2개만 섞음)에 따라 TCP·DevOps만 복습했다.
- 로드맵별 진행률: 8개 중 6개가 진짜 0%(미시작), 네트워크 서비스가 3/251, 데브옵스가 2/799였다.

**여기서 발견한 문제점**: 데브옵스가 실제로는 2개를 체크해 진행 중인데도, 정수 %로 계산하니
`2/799 = 0.25%`가 버림되어 **`0%`로 표시**됐다. 화면과 `브리핑로그.md`에서 "전혀 안 한 로드맵"과
"방금 시작한 로드맵"이 똑같이 `0%`로 보여서, 이 브리핑이 원래 풀려던 문제("어디까지 했는지 한눈에
안 보임")가 진행률이 작을 때는 그대로 재현되고 있었다. (`내학습/브리핑로그.md` 2026-09-16 행의
메모에 이 문제를 그대로 남겼다.)

## 개선

`.claude/skills/브리핑/SKILL.md`의 1절에 % 계산 규칙을 추가했다.

- 완료 0개 → `"0% (미시작)"`
- 완료는 있지만 소수 첫째 자리도 0.0이 되는 경우 → `"<0.1% (진행 중, {완료}/{전체})"`
- 그 외 → 소수 첫째 자리까지, `"완료/전체"`를 항상 %와 나란히 표시

정수 반올림 대신 항상 원래 분수(`완료/전체`)를 %와 같이 보여줘서, 반올림 때문에 "시작 안 함"과
"시작은 했음"이 구별 안 되는 문제를 없앴다.

## 사용 기록 2 — 2026-09-18, `/브리핑` (새 대화, 이어서 사용)

새 세션을 열어 저장된 파일만으로 이어서 진행했다. 그 사이(2026-09-16, 2026-09-17) `/study`로
두 번 더 진행했다고 가정했다: 9/16에 복습큐의 TCP·DevOps 두 항목을 리뷰해서 둘 다 맞혔고(1회차→2회차,
다음 복습일 9/19로 이동), 네트워크 서비스 Phase 1 1-A 나머지 2개도 체크했다. 9/17에는 데브옵스
Phase 1 1-A 나머지 3개를 체크했다(`내학습/진행상황.md`의 9/16, 9/17 행).

**기대한 결과**: (1) 저장된 `내학습/복습큐.md`를 읽어서 리뷰 완료된 두 항목은 더 이상 안 뜨고
NAT 항목만 밀린 것으로 나온다. (2) 개선한 % 표시가 실제로 데브옵스 진행을 가려지지 않게 보여준다.

**실제 결과**:

- 복습 우선순위: NAT 지점 항목만(다음 복습일 9/15, 3일 밀림) 표시됨 — 9/16 세션에서 TCP·DevOps만
  복습하고 NAT는 다루지 않아 그대로 밀려 있었다. TCP·DevOps 항목은 다음 복습일이 9/19라 이번엔 안 떴다 — **"새 대화에서도 저장한 기록을 읽고 이어서 판단한다"**가 기대대로 동작했다.
- 로드맵별 진행률: 네트워크 서비스 `2.0% (5/251)`, 데브옵스 `0.6% (5/799)`로 둘 다 0%에 묻히지 않고
  보였다. 나머지 6개는 여전히 `0% (미시작)`으로 정확히 구별됐다.

**개선 전후 비교**:

| | 개선 전 (9/16) | 개선 후 (9/18) |
|---|---|---|
| 데브옵스 표시 | `0%` (2/799였는데도) | `0.6% (5/799)` |
| "미시작"과 "진행 중"의 구별 | 안 됨 (둘 다 0%) | 됨 (`0% (미시작)` vs `0.6%`) |

## 최소 확인 기준 대조

- 핵심 기능 1개: `/브리핑` (로드맵별 진행률 + 복습 우선순위를 한 번에 보여주고 기록) — 충족
- 사용 기록 2회 이상: 2026-09-16, 2026-09-18 — 충족 (`내학습/브리핑로그.md` 두 행)
- 개선 전후 비교 1건: % 반올림으로 진행 중인 로드맵이 "0%"에 묻히던 문제 → 완료/전체를 %와 같이
  표시하도록 수정 — 충족
- "파일에 저장했다"와 "다음 실행에서 읽고 활용했다": 9/16 실행이 `내학습/복습큐.md`를 갱신했고,
  9/18 실행(새 대화)이 그 갱신된 파일을 읽어 NAT 항목만 골라냈다 — 충족

## 마무리 질문에 대한 답

- **맡긴 일 / 직접 판단하는 일**: 체크박스 세기, 마감일 비교, 표로 정리하는 것은 `/브리핑`에 맡겼다.
  오늘 실제로 무엇을 공부할지, 로드맵을 새로 만들지 말지는 내가 판단한다 — 스킬이 그 선을 넘지 않도록
  "절대 하지 않는 것" 절에 명시해 뒀다.
- **새 대화에서 이어서 일하려면 필요했던 정보**: `내학습/복습큐.md`의 "다음 복습일" 열과
  `study-progress/*/`의 체크박스 상태. 둘 다 파일로 남아 있어서 새 세션이 대화 맥락 없이도 그대로
  읽을 수 있었다.
- **매뉴얼을 고치니 달라진 행동**: 같은 데이터(데브옵스 5/799)를 두고 개선 전엔 "0%"라고 잘라 말했고,
  개선 후엔 "0.6% (5/799)"라고 원래 숫자를 같이 보여줬다. 화면에 나오는 문장 자체가 달라졌다.
- **다른 사람이 쓴다면 바꿔야 할 것**: 지금은 로드맵마다 Phase 문서 형식이 같다고 가정한다(`- [ ]`).
  다른 사람이 체크박스 형식이 다른 자료를 쓰면 "형식 확인 필요" 분기가 더 자주 걸릴 텐데, 지금은
  그 경우 표시만 하고 자동으로 고쳐주지 않는다. 사람이 많아지면 형식을 더 관대하게 인식하도록
  넓혀야 한다.

## 추가 개선 — 2026-09-22, 복습큐 "마스터 완료" 상태 도입

**문제**: 복습 간격표(1→3→7→16→35일)에 종료 상태가 없었다. 5회차(35일 간격)에서 맞혀도
그 다음에 무슨 일이 일어나는지 스킬 어디에도 정의돼 있지 않아, 완전히 익힌 항목이 활성 큐에
계속 남아 브리핑 화면과 다음 복습 우선순위 계산에 영구히 걸리는 문제가 있었다.

**변경**:

- `내학습/복습큐.md`에 "## 마스터 완료" 표를 추가하고, 5회차 통과 시 활성 표에서 그 표로
  항목을 옮기도록 규칙화했다(삭제가 아니라 이동 — 기록은 남긴다).
- `.claude/skills/study/SKILL.md` 6절(간격 반복)에 "5회차에서 맞히면 마스터 완료 표로 옮긴다"
  규칙을 추가했다.
- `.claude/skills/브리핑/SKILL.md` 2절에 "마스터 완료" 섹션은 "다음 복습일" 열이 없으므로
  복습 우선순위 계산에서 제외한다는 안내를 추가했다(안 그러면 그 표를 잘못 읽어 오류를 낼 수 있다).

**아직 확인 못 한 것**: 지금 활성 큐의 최고 회차는 2회차라, 실제로 5회차를 통과해 마스터 완료로
넘어가는 경로는 아직 실사용으로 검증되지 않았다. 다음에 어떤 항목이 5회차 복습을 통과할 때
`/study`가 규칙대로 표를 옮기는지 확인이 필요하다.
